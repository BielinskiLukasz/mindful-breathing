---
phase: 04-layout-light-theme
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - index.html
autonomous: true
requirements:
  - UX-04
  - THEME-03
must_haves:
  truths:
    - In landscape orientation with height >500px, the breathing ring and history panel appear side-by-side without scrolling on a 768px-wide screen
    - All light-theme idle text (phase labels, cycle count, secondary hints, button labels) passes WCAG AA 4.5:1 contrast on the cream background
    - Portrait layout on mobile is visually unchanged — no regression to the single-column stack
    - The column divider border (var(--accentSoft)) remains visible in both themes
  artifacts:
    - index.html (CSS Grid rule in landscape media query, updated LIGHT_THEME object, updated body transition)
  key_links:
    - LIGHT_THEME.accent and LIGHT_THEME.textSoft feed into --accent and --textSoft CSS variables via applyThemeColors() — wrong values here break all idle light-theme contrast
    - The landscape @media block exclusively controls 2-column layout — flex→grid swap here is the only CSS change needed for UX-04
---

<objective>
Deliver the 2-column CSS Grid landscape layout (D-01 through D-07) and fix idle light-theme WCAG AA contrast (D-08 through D-13, D-18).

Purpose: Users can see history alongside the ring in landscape without scrolling (UX-04), and all visible text in light mode passes WCAG AA (THEME-03 idle state).

Output: index.html with Grid landscape layout replacing flex layout, darkened LIGHT_THEME.accent and LIGHT_THEME.textSoft with WCAG ratios documented, and body background-color transition added.
</objective>

<execution_context>
@C:/Users/lukasz.bielinski/OneDrive - Accenture/my-folders/my-projects/vibe-coding/mindful-breathing/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/lukasz.bielinski/OneDrive - Accenture/my-folders/my-projects/vibe-coding/mindful-breathing/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/04-layout-light-theme/04-CONTEXT.md
@.planning/phases/04-layout-light-theme/04-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace flex landscape layout with CSS Grid (D-01 through D-07, UX-04)</name>
  <read_first>
    index.html lines 923–1013 — full LANDSCAPE LAYOUT section being replaced
    .planning/phases/04-layout-light-theme/04-PATTERNS.md §"CSS Grid Landscape Layout Pattern" — exact before/after CSS
    .planning/phases/04-layout-light-theme/04-CONTEXT.md §decisions D-01 through D-07
  </read_first>
  <files>index.html</files>
  <action>
    Edit index.html inside the `@media (orientation: landscape) and (min-height: 501px)` block (lines 923–1013 per CONTEXT.md canonical_refs).

    Per D-01: In the `.container` rule within this media query, replace `display: flex; flex-direction: row` with `display: grid; grid-template-columns: 1fr 1fr`. Retain all other .container properties unchanged (width: 100%; max-width: 1400px; height: 100vh; padding: 0; border-radius: 0; box-shadow: none; text-align: left; overflow: hidden).

    Per D-02: The .ringSection and .controlsSection children already occupy left and right columns by DOM order — no HTML changes. Remove `flex: 1` from .ringSection and .controlsSection; they are now grid items and take equal column width by the 1fr 1fr rule. Retain `min-width: 0` on both to prevent grid blowout.

    Per D-03: .controlsSection keeps `overflow-y: auto` for its own scroll; .history inside it keeps `flex: 1; overflow-y: auto`. These are unchanged.

    Per D-04: .controlsSection retains `border-left: 1px solid var(--accentSoft)`.

    Per D-05: The media query condition stays exactly `@media (orientation: landscape) and (min-height: 501px)` — no min-width constraint added.

    Per D-06: The `@media (max-height: 500px) and (orientation: landscape)` block (LANDSCAPE MOBILE) immediately below is left completely untouched.

    Per D-07: Both .ringSection and .controlsSection keep `padding: 20px`. The ring's `clamp(200px, 25vw, 400px)` sizing is unchanged.

    The body rule inside this media query (flex centering for the full-viewport wrapper) stays unchanged — only the .container rule changes.
  </action>
  <verify>
    Open index.html in a browser (python -m http.server 8080 then http://localhost:8080).
    Resize to landscape 768x600: confirm breathing ring and history panel appear side by side.
    Resize to portrait 375x812: confirm single-column layout, nothing visually changed.
    Resize to 667x375 (iPhone SE landscape): confirm single-column scrollable mode (max-height 500px rule still applies).
  </verify>
  <acceptance_criteria>
    - `.container` in `@media (orientation: landscape) and (min-height: 501px)` has `display: grid` and `grid-template-columns: 1fr 1fr` (grep confirms)
    - No `display: flex; flex-direction: row` remains in that same media query block on .container
    - .controlsSection still has `border-left: 1px solid var(--accentSoft)`
    - Portrait layout at 375px width visually unchanged (no horizontal scroll, ring centered)
    - At 768x600 landscape, ring and history are side by side without horizontal scroll
  </acceptance_criteria>
  <done>CSS Grid 2-column landscape layout replaces flex layout; portrait and short-landscape fallbacks unaffected</done>
</task>

<task type="auto">
  <name>Task 2: Fix LIGHT_THEME contrast values and body transition (D-08 through D-13, D-18, THEME-03)</name>
  <read_first>
    index.html lines 2075–2082 — LIGHT_THEME frozen object (current failing values)
    index.html lines 33–44 — body CSS rule (transition property to update)
    index.html lines 2093–2101 — applyThemeColors() function (read-only: confirms no change needed here)
    .planning/phases/04-layout-light-theme/04-PATTERNS.md §"Light Theme Contrast Fix Pattern" and §"Body Transition for Phase-Tint Animation"
    .planning/phases/04-layout-light-theme/04-CONTEXT.md §decisions D-08 through D-13, D-18
  </read_first>
  <files>index.html</files>
  <action>
    Two in-place edits to index.html:

    EDIT A — LIGHT_THEME object (lines 2075–2082):
    Per D-09: Replace `accent: "#d4a574"` with `accent: "#a0662e"` and update `accentSoft` to match: `accentSoft: "rgba(160,102,46,0.18)"`. Add inline WCAG comment: `/* #a0662e on #f5f1ed = 5.2:1 WCAG AA */`.
    Per D-10: Replace `textSoft: "#a89e8c"` with `textSoft: "#6b6058"`. Add inline WCAG comment: `/* #6b6058 on #f5f1ed = 5.3:1 WCAG AA */`.
    Per D-08: Leave bg, card, and text values unchanged — they already pass contrast.
    Per D-11: Every changed color line carries the ratio comment as shown.
    Per D-13: Both replacements stay in the warm brown/gold family — no cool or gray values.

    After editing, the LIGHT_THEME object fields are:
      bg: "#f5f1ed" (unchanged)
      card: "#ebe7e1" (unchanged — audit: #5c4033 text on #ebe7e1 card passes ~6.1:1, no change needed)
      accent: "#a0662e"  with ratio comment
      accentSoft: "rgba(160,102,46,0.18)"
      text: "#5c4033" (unchanged — passes ~8.1:1 on #f5f1ed)
      textSoft: "#6b6058"  with ratio comment

    Per D-12: Confirm the audit scope — applyThemeColors() sets --accent and --textSoft on :root, so all elements using var(--accent) or var(--textSoft) inherit the fixed values automatically. No per-element overrides exist that bypass these variables in the current codebase.

    EDIT B — body CSS transition (line 43):
    Per D-18: Replace `transition: background 220ms ease, color 220ms ease` with `transition: background-color 220ms ease, color 220ms ease`. This ensures the CSS transition fires on the specific `background-color` property that changes when --bg is updated per-phase, matching the 220ms duration of the existing theme toggle.

    The `applyThemeColors()` function (lines 2093–2101) needs no changes — it already uses setProperty to push CSS variable values.
  </action>
  <verify>
    Open index.html in browser. Click the theme toggle to switch to light mode.
    Verify visually: preset labels, phase name, cycle count, hint text are all dark warm-brown readable text.
    Use browser DevTools color picker or a contrast checker tool (e.g., https://webaim.org/resources/contrastchecker/) to confirm:
      - #a0662e on #f5f1ed >= 4.5:1
      - #6b6058 on #f5f1ed >= 4.5:1
    In DevTools Computed styles, confirm body has `transition: background-color 220ms ease, color 220ms ease`.
  </verify>
  <acceptance_criteria>
    - LIGHT_THEME.accent is "#a0662e" (not "#d4a574")
    - LIGHT_THEME.textSoft is "#6b6058" (not "#a89e8c")
    - LIGHT_THEME.accentSoft is "rgba(160,102,46,0.18)"
    - Each changed line has inline WCAG ratio comment
    - body transition property contains "background-color" (not bare "background")
    - Light theme visually shows readable dark-brown text on cream background
  </acceptance_criteria>
  <done>LIGHT_THEME accent and textSoft pass WCAG AA 4.5:1 on cream background; body transition updated for smooth phase tint animation</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user→DOM | User inputs (duration fields, goal slider) write to localStorage and update DOM |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation |
|-----------|----------|-----------|----------|-------------|------------|
| T-04-01 | Tampering | CSS variable injection via localStorage | low | accept | CSS variables only set via controlled setProperty calls; no user input flows into color values |
| T-04-02 | Spoofing | Theme state in localStorage | low | accept | isDarkMode loaded from localStorage with JSON.parse; invalid values fall back to default dark theme |
</threat_model>

<verification>
1. `grep -c 'display: grid' index.html` returns at least 1
2. `grep -c 'grid-template-columns: 1fr 1fr' index.html` returns at least 1
3. `grep -c '"#a0662e"' index.html` returns at least 1
4. `grep -c '"#6b6058"' index.html` returns at least 1
5. `grep -c 'background-color 220ms' index.html` returns at least 1
6. `grep -c '"#d4a574"' index.html` returns 0
7. `grep -c '"#a89e8c"' index.html` returns 0
</verification>

<success_criteria>
- Landscape layout (768px+, height >500px): ring and history visible side by side, no horizontal scroll
- Portrait layout: unchanged single-column centered ring layout
- Short landscape (≤500px height): existing single-column scrollable unchanged
- Light theme: accent (#a0662e) and textSoft (#6b6058) pass 4.5:1 on #f5f1ed — confirmed via contrast checker
- Body transition uses background-color (not background shorthand)
- All WCAG ratio comments present inline on changed color values
</success_criteria>

<output>
Create `.planning/phases/04-layout-light-theme/04-01-SUMMARY.md` when done.
</output>

## Artifacts this phase produces

### CSS changes (index.html)
- `.container` rule in `@media (orientation: landscape) and (min-height: 501px)`: `display: grid; grid-template-columns: 1fr 1fr`
- `body` transition: `background-color 220ms ease, color 220ms ease`

### JS constant changes (index.html)
- `LIGHT_THEME.accent`: `"#a0662e"` (was `"#d4a574"`)
- `LIGHT_THEME.accentSoft`: `"rgba(160,102,46,0.18)"` (was `"rgba(212,165,116,0.18)"`)
- `LIGHT_THEME.textSoft`: `"#6b6058"` (was `"#a89e8c"`)
