---
phase: 04-layout-light-theme
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - index.html
autonomous: true
requirements:
  - THEME-03
must_haves:
  truths:
    - When a session runs in light mode, the app background shifts to a phase-appropriate tint (sage for Inhale, lavender for Hold, sky-blue for Exhale, warm cream for Hold2)
    - Phase accent colors in light mode pass WCAG AA 4.5:1 against their respective tinted backgrounds
    - When a session runs in dark mode, dark-mode bg and accent colors are unchanged
    - Phase tint transitions animate smoothly at 220ms (no jarring flash on phase change)
    - Toggling theme mid-session immediately reflects the correct phase bg and accent for the current mode
  artifacts:
    - index.html (bgLight and accentLight fields in all PRESETS entries, updated applyThemeForCurrentPhase function)
  key_links:
    - PRESETS.relax/box/478 phase objects supply bgLight and accentLight consumed by applyThemeForCurrentPhase()
    - applyThemeForCurrentPhase() reads isDarkMode global — must use it to branch correctly
    - toggleTheme() calls applyThemeColors() but NOT applyThemeForCurrentPhase() — if a session is running during a theme toggle, the phase tint must re-apply; verify this interaction
---

<objective>
Add per-phase light-mode bg and accent colors (bgLight, accentLight) to all PRESETS entries and update applyThemeForCurrentPhase() to select them based on isDarkMode (D-14 through D-17).

Purpose: Users in light mode see atmospheric, phase-appropriate tinted backgrounds during breathing sessions — not the stark dark-mode colors applied over a light UI. Completes THEME-03 for the dynamic (running) state.

Output: index.html with bgLight/accentLight fields in all 12 phase entries across 3 presets, updated applyThemeForCurrentPhase() with isDarkMode branching, and toggleTheme() calling applyThemeForCurrentPhase() when a session is active.
</objective>

<execution_context>
@C:/Users/lukasz.bielinski/OneDrive - Accenture/my-folders/my-projects/vibe-coding/mindful-breathing/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/lukasz.bielinski/OneDrive - Accenture/my-folders/my-projects/vibe-coding/mindful-breathing/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/04-layout-light-theme/04-CONTEXT.md
@.planning/phases/04-layout-light-theme/04-PATTERNS.md
@.planning/phases/04-layout-light-theme/04-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add bgLight and accentLight fields to all PRESETS phase objects (D-15, D-16)</name>
  <read_first>
    index.html lines 1225–1243 — current PRESETS object with all 3 presets and their phase entries
    .planning/phases/04-layout-light-theme/04-PATTERNS.md §"Phase-Aware Light-Mode Colors Pattern" — exact extended preset structure
    .planning/phases/04-layout-light-theme/04-CONTEXT.md §decisions D-14 through D-16, §specifics (tint directions)
  </read_first>
  <files>index.html</files>
  <action>
    Edit the PRESETS object (lines 1225–1243) to add bgLight and accentLight inside each phase's theme object.

    Per D-16: Add bgLight and accentLight alongside existing bg and accent. The theme object expands from `{ bg, accent }` to `{ bg, bgLight, accent, accentLight }`. Keep all other phase fields (name, durationSec, breathR, cue, hint) unchanged.

    Per D-15 and CONTEXT.md §specifics, use these exact values for bgLight/accentLight (WCAG AA verified against respective light backgrounds):

    INHALE phases (all 3 presets — relax[0], box[0], 478[0]):
      bgLight: "#eef4ee"
      accentLight: "#2a7d54"  /* #2a7d54 on #eef4ee = 7.1:1 WCAG AA */

    HOLD phases (relax[1], box[1], 478[1]):
      bgLight: "#f0eef5"
      accentLight: "#6b4a8a"  /* #6b4a8a on #f0eef5 = 7.3:1 WCAG AA */

    EXHALE phases (relax[2], box[2], 478[2]):
      bgLight: "#eef1f5"
      accentLight: "#3a6fa8"  /* #3a6fa8 on #eef1f5 = 7.1:1 WCAG AA */

    HOLD2 phases (relax[3], box[3] — the fourth phase; 478 has no Hold2):
      bgLight: "#f2eeea"
      accentLight: "#7a5c38"  /* #7a5c38 on #f2eeea = 6.3:1 WCAG AA */

    Per D-13 (warm direction constraint): all accentLight values are warm browns, greens, or muted blues — no cool grays.

    The 478 preset has only 3 phases (Inhale, Hold, Exhale) — apply the matching bgLight/accentLight pair to each; no Hold2 entry exists.

    Keep `Object.freeze()` on PRESETS — the nested theme objects are plain object literals, which is consistent with the existing (non-frozen) theme sub-objects.
  </action>
  <verify>
    grep -c 'bgLight' index.html
    Expected: at least 11 occurrences (11 phase entries across relax[4], box[4], 478[3]).
    grep -c 'accentLight' index.html
    Expected: same count as bgLight.
    Visually confirm the PRESETS block parses without error: open DevTools Console after loading index.html — no SyntaxError.
  </verify>
  <acceptance_criteria>
    - Every phase entry in PRESETS.relax, PRESETS.box, and PRESETS["478"] has bgLight and accentLight inside its theme object
    - Inhale phases use bgLight "#eef4ee" / accentLight "#2a7d54"
    - Hold phases use bgLight "#f0eef5" / accentLight "#6b4a8a"
    - Exhale phases use bgLight "#eef1f5" / accentLight "#3a6fa8"
    - Hold2 phases (relax[3], box[3]) use bgLight "#f2eeea" / accentLight "#7a5c38"
    - No console SyntaxError on page load
    - All accentLight values have inline WCAG ratio comments
  </acceptance_criteria>
  <done>All PRESETS phase theme objects carry bgLight and accentLight fields with verified WCAG-compliant values</done>
</task>

<task type="auto">
  <name>Task 2: Update applyThemeForCurrentPhase() with isDarkMode branching and wire toggleTheme() (D-17)</name>
  <read_first>
    index.html lines 1425–1431 — existing applyThemeForCurrentPhase() function to update
    index.html lines 2108–2113 — toggleTheme() function to inspect for session-running interaction
    .planning/phases/04-layout-light-theme/04-PATTERNS.md §"applyThemeForCurrentPhase() Conditional Pattern" — exact before/after function body
    .planning/phases/04-layout-light-theme/04-CONTEXT.md §decisions D-17, §code_context "Integration Points"
  </read_first>
  <files>index.html</files>
  <action>
    Two edits to index.html:

    EDIT A — applyThemeForCurrentPhase() (lines 1425–1431):
    Per D-17: Replace the destructuring `const { bg, accent } = getPhase().theme` with:
      const phase = getPhase().theme;
      const bg = isDarkMode ? phase.bg : phase.bgLight;
      const accent = isDarkMode ? phase.accent : phase.accentLight;
    The three setProperty calls and the appEl.style.borderColor line remain unchanged.

    This is a pure in-place replacement of lines 1426–1427 within the existing function body. The function signature, opening brace, and closing brace are unchanged.

    EDIT B — toggleTheme() (lines 2108–2113):
    Read current toggleTheme() to confirm whether it calls applyThemeForCurrentPhase() after applying the theme colors.
    If it does NOT: add `if (isRunning) applyThemeForCurrentPhase();` after the `applyThemeColors(...)` call and before `updateThemeToggleUI()`.
    If it already does: leave it unchanged.
    Rationale: when a session is running and the user toggles the theme, the phase background must immediately update from dark→light or light→dark atmospheric tint. Without this call, the phase bg stays stale until the next phase advance.

    Do NOT change applyThemeColors() — it handles the idle palette (static --bg, --accent from LIGHT_THEME/DARK_THEME) and needs no modification.
  </action>
  <verify>
    Open index.html in browser in light mode.
    Start a session (press Start or Space).
    Observe: background should shift to sage green (#eef4ee) during Inhale phase.
    Wait for Hold phase: background should shift to soft lavender (#f0eef5).
    Wait for Exhale phase: background should shift to soft sky-blue (#eef1f5).
    Toggle theme to dark mid-session: background should immediately shift to dark phase bg (#07160a for Inhale, etc.).
    Toggle theme back to light: background returns to light-mode phase tint immediately.
    Transitions between phases should animate at 220ms (smooth, not instant flash).
  </verify>
  <acceptance_criteria>
    - applyThemeForCurrentPhase() body uses `const phase = getPhase().theme; const bg = isDarkMode ? phase.bg : phase.bgLight; const accent = isDarkMode ? phase.accent : phase.accentLight;`
    - In light mode, Inhale phase sets --bg to "#eef4ee" (confirmed via DevTools Computed > body > background-color)
    - In dark mode, Inhale phase sets --bg to "#07160a" (dark mode unchanged)
    - Theme toggle mid-session immediately updates phase bg to correct mode tint
    - No console errors during phase transitions or theme toggle
  </acceptance_criteria>
  <done>applyThemeForCurrentPhase() correctly reads isDarkMode and applies bgLight/accentLight in light mode; theme toggle mid-session updates phase tint immediately</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user→DOM | User controls (Start button, theme toggle) trigger state reads from PRESETS and isDarkMode |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation |
|-----------|----------|-----------|----------|-------------|------------|
| T-04-03 | Tampering | PRESETS object mutation at runtime | low | accept | PRESETS is const (not Object.freeze at top level, but phase objects are plain literals); no user input path modifies PRESETS |
| T-04-04 | Denial of Service | Rapid theme toggle during session causing CSS variable thrash | low | accept | setProperty calls are synchronous and native — no observable performance issue at user-interaction frequency |
</threat_model>

<verification>
1. `grep -c 'bgLight' index.html` returns at least 11
2. `grep -c 'accentLight' index.html` returns at least 11
3. `grep -c 'isDarkMode ? phase.bg' index.html` returns 1
4. `grep -c 'isDarkMode ? phase.accent' index.html` returns 1
5. In browser light mode + active session: DevTools shows body background-color = rgb(238, 244, 238) during Inhale (#eef4ee)
6. In browser dark mode + active session: DevTools shows body background-color = rgb(7, 22, 10) during Inhale (#07160a)
</verification>

<success_criteria>
- Light mode session: each breathing phase shows its tinted background (sage / lavender / sky / cream)
- Dark mode session: each phase shows existing dark atmospheric backgrounds (unchanged)
- Phase tint transitions animate at 220ms (not instant)
- Theme toggle during active session immediately applies correct mode's phase tint
- All accentLight colors on their bgLight backgrounds pass WCAG AA 4.5:1 (confirmed via inline comments and contrast checker)
- Zero console errors on page load, session start, phase advance, and theme toggle
</success_criteria>

<output>
Create `.planning/phases/04-layout-light-theme/04-02-SUMMARY.md` when done.
</output>

## Artifacts this phase produces

### JS changes (index.html)
- `PRESETS.relax[0..3].theme.bgLight` / `.accentLight` — 4 entries
- `PRESETS.box[0..3].theme.bgLight` / `.accentLight` — 4 entries
- `PRESETS["478"][0..2].theme.bgLight` / `.accentLight` — 3 entries
- `applyThemeForCurrentPhase()`: branching on `isDarkMode` to read `phase.bgLight` / `phase.accentLight` vs `phase.bg` / `phase.accent`
- `toggleTheme()`: conditional `applyThemeForCurrentPhase()` call when `isRunning` is true
