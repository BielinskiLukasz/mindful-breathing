---
phase: 07-landscape-layout-ux-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - index.html
autonomous: true
requirements:
  - LAYOUT-02
  - LAYOUT-03
  - LAYOUT-04
  - LAYOUT-05
  - LAYOUT-06

must_haves:
  truths:
    - Fullscreen button renders inside the `.cornerControls` top-left cluster in landscape — no button in the top-right corner
    - Session info row (cycle count + elapsed time) appears below the ring in the left column in landscape
    - Start/Reset buttons sit at exactly 50% of the right column height in landscape
    - No layout element overlaps or clips at 600px, 768px, 1024px, 1440px landscape widths
    - Portrait layout is visually identical to pre-phase state
    - Compact landscape (`max-height: 500px`) activates correctly on height — no changes required there
  artifacts:
    - index.html with fullscreenBtn as last child inside `.cornerControls` div
    - index.html with `#landscapeInfo` div (containing `#cycleLandscape` + `#elapsedLandscape`) inside `.ringSection`
    - index.html with `.controlsSection { position: relative }` and `.controls { position: absolute; top: 50%; transform: translateY(-50%) }` in landscape media query
  key_links:
    - `#cycleLandscape` and `#elapsedLandscape` must exist in DOM before 07-02 adds JS DOM refs
    - `.cornerControls` flex column inherits layout to fullscreenBtn — no custom positioning needed on the button
    - Landscape media query `@media (orientation: landscape) and (min-height: 501px)` at roughly line 956 is the only place to add landscape rules
---

<objective>
Implement all HTML structure and CSS layout changes for Phase 7.

Purpose: Eliminate landscape overlap issues by moving fullscreenBtn into the cornerControls cluster (D-01), adding a duplicate session-info row below the ring (D-03/D-04/D-05 HTML portion), and centering the Start/Reset controls at exactly 50% of the right column height (D-06/D-07). LAYOUT-06 is verified by inspection — no code change needed.

Output: A modified index.html with three structural layout changes, all landscape-only, portrait unchanged.
</objective>

<execution_context>
@C:/Users/lukasz.bielinski/OneDrive - Accenture/my-folders/my-projects/vibe-coding/mindful-breathing/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/lukasz.bielinski/OneDrive - Accenture/my-folders/my-projects/vibe-coding/mindful-breathing/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-landscape-layout-ux-polish/07-CONTEXT.md

Read the relevant sections of index.html before editing. The file is ~2,515 lines. Key anchor points (search by string, not line number):
- Fullscreen CSS section: search `/* ====== FULLSCREEN ====== */`
- cornerControls HTML: search `<div class="cornerControls">`
- fullscreenBtn HTML: search `<button class="fullscreenBtn" id="fullscreenBtn"`
- ringSection HTML: search `<div class="ringSection">`
- nextPhase element: search `<div class="nextPhase" id="nextPhase"`
- Landscape media query: search `/* ====== LANDSCAPE LAYOUT (50/50 SPLIT) ====== */`
- Compact landscape media query: search `/* ====== LANDSCAPE MOBILE ====== */`
- `.controlsSection` CSS in landscape: search `Controls/History side (right 50%)`
- `.controls` CSS in landscape: search `History scrolls above`
</context>

<tasks>

<task type="auto">
  <name>Task 1: Move fullscreenBtn inside cornerControls (D-01, D-02)</name>
  <files>index.html</files>
  <action>
    Two edits: one HTML, one CSS.

    HTML EDIT — Move the fullscreenBtn element:
    The `<button class="fullscreenBtn" id="fullscreenBtn" ...>` element currently sits immediately AFTER the closing `</div>` of `.cornerControls` (after the `themeToggleBtn` line). Move it to be the LAST child inside `.cornerControls`, before its closing `</div>`. The element to move is the full block starting at `<button class="fullscreenBtn" id="fullscreenBtn"` and ending at the matching `</button>`. It contains two `<svg>` children (`fsEnterIcon` and `fsExitIcon`).

    After the move, the inside of `.cornerControls` should end with:
    `themeToggleBtn` button → `fullscreenBtn` button → `</div>` (closing cornerControls)

    CSS EDIT — Strip absolute positioning from `.fullscreenBtn`:
    In the `/* ====== FULLSCREEN ====== */` CSS section, locate `.fullscreenBtn { ... }`. Remove exactly these two declarations:
      `position: absolute;`
      `top: 10px;`
      `right: 10px;`
    Leave all other declarations untouched: width, height, min-width, padding, background, border, border-radius, color, display, align-items, justify-content.

    The button now inherits `.cornerControls` layout (flex column, gap: 6px). It needs no custom positioning.

    DO NOT touch: the `.fullscreenBtn:hover` rule, the compact landscape `.fullscreenBtn { width: 32px; height: 32px; }` rule in `@media (max-height: 500px)`, or any JS referencing `fullscreenBtn`.

    Per D-02: `.cornerControls` keeps `position: absolute; top: 10px; left: 10px`. Verify the cluster does not overlap the ring visually after adding a fifth button — if the cluster grows taller than the ring, increase the top offset from 10px to 14px only if overlap is observed during verify.

    Note on LAYOUT-02: The requirement text mentions "below the ring" but D-01 (locked user decision) places fullscreenBtn in the top-left cornerControls cluster instead. D-01 takes precedence. This resolves the original overlap problem (button floating over the landscape grid at top-right) while keeping all icon controls together.
  </action>
  <verify>
    Serve `python -m http.server 8080`, open http://localhost:8080, rotate to landscape (min-height 501px). Confirm:
    - Top-left cluster shows 5 buttons stacked vertically (info, settings, sound, theme, fullscreen)
    - No button appears at top-right corner
    - Clicking the fullscreen button still toggles fullscreen (JS unchanged, `id="fullscreenBtn"` intact)
    - Portrait: cornerControls cluster still visible at top-left, fullscreenBtn included
  </verify>
  <done>
    No `position: absolute; top: 10px; right: 10px` declarations remain on `.fullscreenBtn` in CSS. The `fullscreenBtn` HTML element is the last child of `.cornerControls`. Top-right corner is clear of any floating button in landscape.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add landscape info row below ring (D-03, D-04, D-05 HTML/CSS)</name>
  <files>index.html</files>
  <action>
    Three edits: one HTML addition, one CSS addition in global scope, one CSS addition inside the landscape media query.

    HTML EDIT — Add landscapeInfo div inside ringSection:
    Locate `<div class="nextPhase" id="nextPhase"></div>` inside `.ringSection`. Immediately AFTER that line, insert:

      `<div class="landscapeInfo" id="landscapeInfo">`
      `  <span class="cycle" id="cycleLandscape"></span>`
      `  <span class="elapsed" id="elapsedLandscape">0:00</span>`
      `</div>`

    `#cycleLandscape` uses the `.cycle` class to inherit existing font-size and color. `#elapsedLandscape` uses the `.elapsed` class for tabular-nums styling. Both start empty/0:00 — JS in plan 07-02 will populate them on every render cycle.

    CSS EDIT — Hide in portrait (global scope):
    After the existing `.elapsed { ... }` rule block (search `font-variant-numeric: tabular-nums`), add a new rule:

      `#landscapeInfo { display: none; }`

    This hides both the container and its children in portrait by default.

    CSS EDIT — Show in landscape, hide portrait duplicates:
    Inside `@media (orientation: landscape) and (min-height: 501px)`, after the `.ringSection` rule block, add:

      `/* Info row below ring — landscape only (D-03, D-04, D-05) */`
      `#cycle { display: none; }`
      `#elapsed { display: none; }`
      `#landscapeInfo {`
      `  display: flex;`
      `  gap: 16px;`
      `  align-items: center;`
      `  justify-content: center;`
      `  margin-top: 10px;`
      `}`

    Hiding `#cycle` removes the above-ring cycle count in landscape (per D-04 — use display:none approach).
    Hiding `#elapsed` removes elapsed from the statusRow in landscape (the duplicate `#elapsedLandscape` takes its place).
    The `.status` div in `.statusRow` (running dot + "Stopped/Running" text) remains visible — only `#elapsed` span is hidden.

    DO NOT hide `.statusRow` itself — only `#elapsed` inside it.
    DO NOT change portrait CSS in any way.
  </action>
  <verify>
    In landscape (min-height 501px): below the breathing ring, the left column shows cycle text (e.g., "Cycle: 1 / 5") and elapsed time (e.g., "0:00") in a horizontal row. The original `#cycle` above the ring is not visible. The statusRow in the right column shows running status dot and label but no elapsed time.
    In portrait: the info row is not visible; `#cycle` shows above ring as before; elapsed appears in statusRow as before.
  </verify>
  <done>
    `#landscapeInfo` div with `#cycleLandscape` and `#elapsedLandscape` children exists in HTML inside `.ringSection`. Global CSS hides it with `display: none`. Landscape media query shows it and hides `#cycle` and `#elapsed`. No portrait layout change.
  </done>
</task>

<task type="auto">
  <name>Task 3: Center Start/Reset controls at 50% column height (D-06, D-07)</name>
  <files>index.html</files>
  <action>
    Two CSS edits inside `@media (orientation: landscape) and (min-height: 501px)`.

    CSS EDIT — Make controlsSection a positioning context:
    Locate the `.controlsSection { ... }` rule inside the landscape media query (search `Controls/History side (right 50%)`). Add `position: relative;` to this rule. All other existing declarations remain (min-width, display, flex-direction, justify-content, padding, overflow-y, border-left).

    CSS EDIT — Float controls at exact 50% of column height:
    Locate the `.controls { ... }` rule inside the landscape media query (search `History scrolls above; controls/status/bar fixed at bottom`). Replace its landscape-specific declarations with:

      `position: absolute;`
      `top: 50%;`
      `left: 0;`
      `right: 0;`
      `transform: translateY(-50%);`
      `padding: 0 20px;`
      `border-top: none;`
      `padding-top: 0;`
      `margin-top: 0;`

    Remove `order: 2` (not meaningful when position:absolute takes element out of flex flow).
    Keep `flex-shrink: 0`, `justify-content: flex-start`, `flex-wrap: wrap` — these govern the internal Start/Reset button layout.

    The history (flex:1, order:1), statusRow (order:3), and sessionBar (order:4) remain as flex children and continue to fill the column naturally. The `.controls` box floats on top at exactly the column midpoint via the absolute + translateY approach specified in D-07.

    LAYOUT-06 VERIFICATION (no code change needed):
    Locate `/* ====== LANDSCAPE MOBILE ====== */` and confirm the media query condition reads `@media (max-height: 500px) and (orientation: landscape)`. This is a height-based trigger, satisfying LAYOUT-06 as-is. Document in the task summary that LAYOUT-06 was verified as already compliant with no changes required.
  </action>
  <verify>
    In landscape at each of 600px, 768px, 1024px, 1440px wide:
    - Start/Reset buttons appear at the vertical midpoint of the right column (roughly equidistant from top and bottom edges)
    - History scrollable area is visible above and/or around the controls without clipping
    - Status row (running dot + label) and session bar are visible at the bottom of the right column
    - No elements overlap or clip each other

    Confirm compact landscape: narrow viewport to max-height 500px in landscape — the compact mobile layout activates (single-column scroll). Confirm it uses `@media (max-height: 500px) and (orientation: landscape)` (height-based, no change needed).
  </verify>
  <done>
    `.controlsSection` has `position: relative` in landscape CSS. `.controls` has `position: absolute; top: 50%; transform: translateY(-50%)` in landscape CSS with no `order` or `border-top`. Start/Reset buttons render at the vertical center of the right column. History, statusRow, and sessionBar continue to use the flex layout normally. LAYOUT-06 verified as height-based (no code change).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| localStorage → DOM | Saved settings loaded at startup and rendered into the UI |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-01 | Tampering | CSS absolute positioning on `.controls` | low | accept | Controls float at 50% via CSS only — no user input involved; no data path affected |
| T-07-02 | Information Disclosure | `#elapsedLandscape` / `#cycleLandscape` duplicate elements | low | accept | Elements duplicate already-visible DOM data; no new information exposure |
</threat_model>

<verification>
After all three tasks complete, verify end-to-end in a browser:

1. Open http://localhost:8080 in desktop Chrome/Edge
2. Landscape (min-height 501px): cornerControls top-left shows 5 icon buttons; no button at top-right
3. Landscape: below the ring — cycle count and elapsed time visible; `#cycle` above ring is hidden
4. Landscape at 600px, 768px, 1024px, 1440px: Start/Reset buttons centered at column midpoint; no overlaps
5. Portrait: layout identical to pre-phase — cycle count above ring, elapsed in statusRow, fullscreenBtn in top-left cluster
6. Compact landscape (≤500px height): mobile layout activates — verify by setting DevTools to iPhone SE landscape
</verification>

<success_criteria>
- No `position: absolute; top: 10px; right: 10px` in the `.fullscreenBtn` CSS rule
- `fullscreenBtn` is the last child of `.cornerControls` in HTML
- `#landscapeInfo` containing `#cycleLandscape` and `#elapsedLandscape` exists in `.ringSection` after `#nextPhase`
- Global CSS has `#landscapeInfo { display: none }`
- Landscape CSS hides `#cycle` and `#elapsed`, shows `#landscapeInfo`
- `.controlsSection` has `position: relative` in landscape CSS
- `.controls` has `position: absolute; top: 50%; transform: translateY(-50%)` in landscape CSS
- Portrait: visually unchanged from Phase 6 output
</success_criteria>

<output>
Create `.planning/phases/07-landscape-layout-ux-polish/07-01-SUMMARY.md` when done.
</output>
