---
phase: 09-custom-preset-builder
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - index.html
autonomous: true
gap_closure: true
gap_ids:
  - G-09-3a
  - G-09-3b
requirements: []
user_setup: []

must_haves:
  truths:
    - "Name error message clears when user types a valid name after a failed save attempt"
    - "Validation error for fewer than 2 phases remains visible for at least 3 seconds"
  artifacts:
    - "index.html with presetNameError cleared at start of submit handler"
    - "index.html with phaseCountError timeout set to 3000ms"
  key_links:
    - "presetNameError.style.display at line 2062 (after e.preventDefault)"
    - "setTimeout visibility hide for phaseCountError at line 2081"
---

<objective>
Fix validation message UX in the custom preset builder form. Users were unable to see validation errors (disappeared too quickly) and form errors persisted after correcting input.

Purpose: Ensure validation feedback is clear and readable
Output: Updated index.html with correct error message timing and clearing behavior
</objective>

<execution_context>
@C:/my-code/vibe-coding/mindful-breathing/.claude/gsd-core/workflows/execute-plan.md
@C:/my-code/vibe-coding/mindful-breathing/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@C:/my-code/vibe-coding/mindful-breathing/index.html (lines 2050–2100)
</context>

<tasks>

<task type="auto">
  <name>Fix G-09-3a: Clear name error before re-validating on submit</name>
  <files>index.html</files>
  <action>
In the preset builder submit handler (around line 2062), add a single line to clear the error element display at the start of validation. This fixes the issue where error messages persisted even after the user typed a valid name.

Locate the submit handler `presetBuilderForm.addEventListener("submit", e => {` around line 2060. Immediately after `e.preventDefault();` (line 2062), add:

```
presetNameError.style.display = "none";
```

This ensures the error is hidden before the validation logic re-runs, so if validation passes on the second attempt, the user sees no error. If validation fails again, a fresh error message is shown.

Per G-09-3a root cause: presetNameError is not cleared at the start of the submit handler.
  </action>
  <verify>
    <automated>grep -n "presetNameError.style.display = \"none\"" index.html | grep -E "206[0-9]" | wc -l</automated>
  </verify>
  <done>Line added at or near line 2062 (within ~5 lines after e.preventDefault). Grep finds the exact string in the 2060-2069 range.</done>
</task>

<task type="auto">
  <name>Fix G-09-3b: Increase phaseCountError visibility timeout to 3 seconds</name>
  <files>index.html</files>
  <action>
In the same submit handler, locate the setTimeout that hides phaseCountError (around line 2081). The current timeout is 400ms, which is too brief for users to read. Change it to 3000ms (3 seconds).

Find: `setTimeout(() => phaseCountError.style.display = "none", 400)`
Replace: `setTimeout(() => phaseCountError.style.display = "none", 3000)`

Per G-09-3b root cause: setTimeout duration is 400ms, too short to read.
  </action>
  <verify>
    <automated>grep -n "phaseCountError.style.display = \"none\", 3000" index.html | wc -l</automated>
  </verify>
  <done>The 3000ms timeout is set for phaseCountError visibility. Grep finds exactly 1 match for the updated timeout value.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

None — both changes are internal form validation timing, no security implications.

## STRIDE Threat Register

No security threats identified for this plan. Changes are UI/UX only.
</threat_model>

<verification>
1. Open the app in a browser
2. Click the '+' button to open the preset builder
3. Leave name empty, click Save → error should appear "Name required"
4. Type a name, click Save → error should disappear
5. Clear the name again, click Save → error appears
6. Repeat step 3, then uncheck all phase checkboxes except one, click Save
7. Verify "At least 2 phases required" error message appears
8. Wait 3+ seconds → error should remain visible (not disappear in <1 sec)
</verification>

<success_criteria>
- Name validation error message clears when user types a valid name and re-submits
- Phase count validation error remains visible for at least 3 seconds, giving users time to read and respond
</success_criteria>

<output>
Create `.planning/phases/09-custom-preset-builder/09-04-SUMMARY.md` when done
</output>
