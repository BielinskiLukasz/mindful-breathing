---
phase: 09-custom-preset-builder
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - index.html
autonomous: true
gap_closure: true
gap_ids:
  - G-09-9
  - G-09-10
  - G-09-11
  - G-09-12
  - G-09-13
  - G-09-20a
  - G-09-20b
requirements: []
user_setup: []

must_haves:
  truths:
    - "Clicking the edit icon (✎) on a custom preset button opens the builder dialog in edit mode (desktop)"
    - "Edit dialog is centered on screen, matching the settings panel positioning"
    - "Edit dialog pre-fills with preset name, phase checkboxes, durations, and shows Delete button"
    - "Editing a preset updates its button and active phases immediately"
    - "Editing the active preset rebuilds phases without needing to reselect"
  artifacts:
    - "index.html with line 1882 (editIcon.style.pointerEvents) removed"
    - "index.html with #presetBuilderDialog CSS centering applied (fixed position, translate transform)"
  key_links:
    - "Remove pointerEvents='none' at line 1882 — existing e.stopPropagation() already prevents bubbling"
    - "Edit icon click listener at line 1922 must be triggered when edit icon is clicked"
    - "#presetBuilderDialog CSS must include position:fixed, top:50%, left:50%, transform:translate(-50%, -50%)"
---

<objective>
Fix desktop edit icon click handler and dialog centering. The edit icon was blocked from receiving clicks due to pointerEvents:none, preventing users from opening the edit dialog via desktop. Additionally, the dialog appeared in the top-left corner instead of centered like the settings panel.

Purpose: Enable preset editing on desktop and improve visual UX with centered dialog positioning
Output: Updated index.html with click handler enabled and dialog CSS centered
</objective>

<execution_context>
@C:/my-code/vibe-coding/mindful-breathing/.claude/gsd-core/workflows/execute-plan.md
@C:/my-code/vibe-coding/mindful-breathing/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@C:/my-code/vibe-coding/mindful-breathing/index.html (lines 1875–1930, 235–243)
</context>

<tasks>

<task type="auto">
  <name>Fix G-09-9/10/11/12/13/20a: Remove pointerEvents:none to enable edit icon click listener</name>
  <files>index.html</files>
  <action>
The edit icon click listener at line 1922 is blocked from receiving clicks because line 1882 sets `editIcon.style.pointerEvents = "none"`. This line was intended to avoid interfering with button clicks, but it also blocks the click listener on the icon itself.

Remove or comment out line 1882 entirely:

```javascript
// editIcon.style.pointerEvents = "none";  // REMOVED — unnecessary; e.stopPropagation() handles bubbling
```

Alternatively, delete the line completely. The existing `e.stopPropagation()` at line 1924 (inside the edit icon click handler) already prevents the click from bubbling to the preset selection handler, so there is no need to disable pointer events.

Per G-09-9/10/11/12/13/20a root cause: editIcon.style.pointerEvents = 'none' blocks the click listener at line 1922.
  </action>
  <verify>
    <automated>grep -c "editIcon.style.pointerEvents" index.html</automated>
  </verify>
  <done>The pointerEvents assignment line is removed (grep finds 0 matches for the exact pattern, or only matches in comments if present). The edit icon click listener at line 1922 is now reachable.</done>
</task>

<task type="auto">
  <name>Fix G-09-20b: Center the preset builder dialog on screen</name>
  <files>index.html</files>
  <action>
The #presetBuilderDialog CSS rule (lines 235–243) currently has only `margin: 24px` and other properties, which does not center a <dialog> element. The dialog appears in the top-left corner instead of centered like the settings panel.

Locate the #presetBuilderDialog CSS rule. Update or add the following properties to center the dialog:

```css
#presetBuilderDialog {
  position: fixed;
  top: 50%;
  left: 50%;
  transform: translate(-50%, -50%);
  /* existing properties below */
}
```

If `margin: 24px` is present in the rule, remove it — it conflicts with the fixed centering. The dialog should have padding/sizing for inner content, but positioning is handled by the fixed position + transform.

Per G-09-20b root cause: #presetBuilderDialog missing position:fixed + top/left/transform centering.
  </action>
  <verify>
    <automated>grep -A 6 "#presetBuilderDialog {" index.html | grep -c "transform: translate"</automated>
  </verify>
  <done>The #presetBuilderDialog rule now includes `transform: translate(-50%, -50%)` for centering. Grep finds exactly 1 match for the transform property in the rule.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

None — all changes are UI interaction and layout. No security implications.

## STRIDE Threat Register

No security threats identified for this plan. Changes affect only edit icon interactivity and dialog positioning.
</threat_model>

<verification>
1. Open the app, navigate to settings
2. Create a custom preset "Test Preset" with Inhale+Hold phases, 4s each
3. Verify the button appears in the custom presets row
4. On desktop (or DevTools desktop emulation), hover over the "Test Preset" button
5. Verify an edit icon (✎) appears on the button (long-press works on mobile, confirmed in test 22)
6. Click the edit icon → verify the builder dialog opens
7. Verify the dialog is centered on the screen (not top-left)
8. Verify the preset name field shows "Test Preset" (pre-filled)
9. Verify "Inhale" and "Hold" checkboxes are checked, others are unchecked
10. Verify duration inputs show 4s for Inhale and Hold
11. Verify a "Delete" button is visible (not shown in create mode)
12. Edit the name to "Updated Preset", change Inhale to 5s, click Save
13. Verify the button label changes to "Updated Preset"
14. Verify the Inhale duration is now 5s when the preset is active
15. Verify changes persist after page reload
</verification>

<success_criteria>
- Edit icon click opens the builder dialog in edit mode on desktop
- Dialog is centered on screen matching the settings panel positioning
- Edit dialog pre-fills all saved preset data (name, checked phases, durations)
- Delete button appears in edit mode
- Editing a preset updates the button label and active phases immediately
- Changes persist after page reload
- Long-press edit (mobile) continues to work as before
</success_criteria>

<output>
Create `.planning/phases/09-custom-preset-builder/09-06-SUMMARY.md` when done
</output>
