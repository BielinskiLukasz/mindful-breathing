---
phase: 10.1
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [index.html]
autonomous: true
requirements: [A11Y-01, HIST-12]

must_haves:
  truths:
    - "Pressing Escape on preset builder dialog closes the dialog AND clears keyboard shortcut blocking"
    - "Space/R/F keyboard shortcuts work immediately after pressing Escape on preset builder"
    - "Streak badge and panel update immediately after importing JSON or CSV history"
    - "Longest streak recalculates and persists if imported history reveals a longer streak"
  artifacts:
    - "index.html with presetBuilderDialog cancel event listener"
    - "index.html with updateStreakBadge(), updateStreakPanel(), maybeUpdateLongestStreak() called after importJson() renderHistory()"
    - "index.html with updateStreakBadge(), updateStreakPanel(), maybeUpdateLongestStreak() called after importCsv() renderHistory()"
  key_links:
    - "presetBuilderDialog cancel event → openPanelElement = null → anyPanelOpen clears → Space/R/F unblocked"
    - "importJson/importCsv renderHistory() → updateStreakBadge/Panel/LongestStreak chain → streak display live"
---

<objective>
Fix two critical integration gaps from v1.1 milestone audit:
1. **A11Y-01 Keyboard Navigation Blocker**: Closing preset builder via Escape does not reset keyboard shortcut block state
2. **Streak Badge Stale After Import**: Importing CSV/JSON history does not refresh streak display

Purpose: Complete keyboard accessibility (A11Y-01) and ensure streak consistency after imports
Output: Two targeted code additions in index.html fixing cross-phase integration
</objective>

<execution_context>
@C:/my-code/vibe-coding/mindful-breathing/.claude/gsd-core/workflows/execute-plan.md
@C:/my-code/vibe-coding/mindful-breathing/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/v1.1-MILESTONE-AUDIT.md
@.planning/phases/10.1-close-gap-a11y-01-preset-builder-cancel-event-streak-badge-a/10.1-CONTEXT.md
@.planning/phases/10-streak-tracking/10-CONTEXT.md
@.planning/phases/08-accessibility-csv-import/08-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Fix 1: Add preset builder dialog cancel event listener (D-01, D-02, D-03)</name>
  <files>index.html</files>
  <read_first>
    @index.html — read lines 2040–2120 to see where presetBuilderDialog is referenced and where the form handlers are attached
    @.planning/v1.1-MILESTONE-AUDIT.md — "Fix (single line)" in BLOCKER section shows exact fix approach
    @.planning/phases/10.1-close-gap-a11y-01-preset-builder-cancel-event-streak-badge-a/10.1-CONTEXT.md — D-01 through D-04 decisions
  </read_first>
  <action>
Per D-01: Add a native `cancel` event listener to `presetBuilderDialog` (the HTML `<dialog>` element).

Locate the dialog initialization block (around line 2051 where presetBuilderDialog is first referenced). After line 2056 (after presetCancelBtn is assigned), add a new event listener:

```
presetBuilderDialog.addEventListener("cancel", () => {
  openPanelElement = null;
  document.getElementById("newPresetBtn")?.focus();
});
```

Per D-02, D-03: The listener must set `openPanelElement = null` (to clear the keyboard shortcut block state) and restore focus to `newPresetBtn` (consistent with all other panel close handlers: infoBtn, settingsBtn, streakBtn).

Do NOT add an Escape branch to the keydown handler (D-04) — the cancel event is the cleaner pattern for native `<dialog>` elements.

Verify by searching index.html for existing cancel listeners in other dialog elements (none should exist currently); this is the only new listener added.
  </action>
  <verify>
    <automated>grep -n "presetBuilderDialog.addEventListener.*cancel" C:/my-code/vibe-coding/mindful-breathing/index.html | head -1</automated>
  </verify>
  <done>
    - grep finds exactly one `presetBuilderDialog.addEventListener("cancel"` line in index.html
    - The listener handler sets `openPanelElement = null` AND calls `document.getElementById("newPresetBtn")?.focus()`
    - No Escape branch exists in the keydown handler for presetBuilderDialog
  </done>
  <acceptance_criteria>
    - Source assertion: index.html contains `presetBuilderDialog.addEventListener("cancel", () => { openPanelElement = null; document.getElementById("newPresetBtn")?.focus(); });`
    - Behavior assertion: User opens preset builder → presses Escape → dialog closes → Space key immediately starts session (keyboard shortcut unblocked)
    - Behavior assertion: Multiple Escape presses in a row do not cause errors (handler is idempotent)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Fix 2: Call streak update functions after import history load (D-05, D-06, D-07)</name>
  <files>index.html</files>
  <read_first>
    @index.html — read lines 2460–2520 (importJson function) and lines 2523–2620 (importCsv function) to find exact placement points
    @index.html — search for existing streak update pattern: lines 3070–3072 (saveHistory path) and lines 3608–3610 (startup path) show the three-call pattern
    @.planning/v1.1-MILESTONE-AUDIT.md — "Fix (two lines, in each import function)" section in WARNING subsection
    @.planning/phases/10.1-close-gap-a11y-01-preset-builder-cancel-event-streak-badge-a/10.1-CONTEXT.md — D-05, D-06, D-07 decisions
  </read_first>
  <action>
Per D-05, D-06, D-07: After `renderHistory()` is called in BOTH importJson() and importCsv(), add three function calls in strict order:
1. `updateStreakBadge()`
2. `updateStreakPanel()`
3. `maybeUpdateLongestStreak()`

**In importJson():**
Locate line 2503 where `renderHistory();` is called (after localStorage.setItem and before sessionStorage.setItem).
Insert the three calls immediately after `renderHistory();` on line 2503:
```
updateStreakBadge();
updateStreakPanel();
maybeUpdateLongestStreak();
```

**In importCsv():**
Locate line 2609 where `renderHistory();` is called (after localStorage.setItem and before sessionStorage.setItem).
Insert the same three calls immediately after `renderHistory();` on line 2609:
```
updateStreakBadge();
updateStreakPanel();
maybeUpdateLongestStreak();
```

Per D-07: This matches the established pattern used at session end (Phase 10 code, lines 3070–3072) and at app startup (lines 3608–3610). The order is critical: badge first (visual feedback), then panel content (if open), then persistence check.

Per D-06: Calling maybeUpdateLongestStreak() is required because importing historical sessions could reveal a streak longer than the current persisted high-water mark — for example, importing a CSV with a 30-day gap before today, then a 25-day consecutive streak from 6 months ago, would update longestStreak even though currentStreak is lower.
  </action>
  <verify>
    <automated>grep -n "renderHistory()" C:/my-code/vibe-coding/mindful-breathing/index.html | grep -E "250[0-9]|260[0-9]" | head -2</automated>
  </verify>
  <done>
    - grep finds exactly two renderHistory() calls in the import functions (one in importJson, one in importCsv)
    - Each renderHistory() call is followed by updateStreakBadge(), updateStreakPanel(), maybeUpdateLongestStreak() in that order
    - The three-call block is placed BEFORE sessionStorage.setItem("historyPage", "1") in both functions
  </done>
  <acceptance_criteria>
    - Source assertion: importJson() contains renderHistory() followed by updateStreakBadge(), updateStreakPanel(), maybeUpdateLongestStreak() at lines ~2503–2506
    - Source assertion: importCsv() contains renderHistory() followed by updateStreakBadge(), updateStreakPanel(), maybeUpdateLongestStreak() at lines ~2609–2612
    - Behavior assertion: User imports 10 JSON sessions → streak badge immediately shows correct current streak count (not stale "0")
    - Behavior assertion: User imports CSV with older consecutive sessions → longest streak persists to localStorage if higher than before
    - Behavior assertion: Importing zero sessions (all duplicates) still calls the three functions without error
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User input → Import file parser | File may be malformed or contain untrusted data; already handled by try/catch in both importJson/importCsv |
| Imported session dates → Streak computation | Dates already validated by importJson/importCsv before calling renderHistory; no new validation needed |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10.1-01 | Tampering | presetBuilderDialog.cancel event listener | low | accept | Event fires only after user presses Escape; no external trigger. Native browser event, not user-controlled data. |
| T-10.1-02 | Information Disclosure | maybeUpdateLongestStreak re-reads history | low | accept | Re-reading existing localStorage history does not expose new data; existing risk model from Phase 10 applies. |

</threat_model>

<verification>
**Phase 10.1 Verification Scope:**

1. **A11Y-01 Keyboard Integration**: After applying Fix 1, open preset builder → press Escape → immediately press Space. Expected: session starts. Actual: must observe session start.

2. **Import Streak Refresh**: After applying Fix 2, import a CSV or JSON file with 5+ sessions → verify streak badge updates immediately to show correct count (not stuck at previous value).

3. **Longest Streak Persistence**: Import CSV with sessions from 6 months ago showing a 20-day streak, then import recent sessions. Verify localStorage longestStreak persists if higher than before.

4. **No Regressions**: All existing preset builder close paths (Cancel button, Save button, Delete action) still work correctly and clear keyboard shortcut state.

5. **No Regressions**: Normal session import (1–2 sessions) still shows correct feedback message and streak updates.
</verification>

<success_criteria>
Phase 10.1 complete when:
- [ ] Fix 1: Escape key listener added to presetBuilderDialog, sets openPanelElement = null, focuses newPresetBtn
- [ ] Fix 2: updateStreakBadge(), updateStreakPanel(), maybeUpdateLongestStreak() added after renderHistory() in importJson()
- [ ] Fix 2: updateStreakBadge(), updateStreakPanel(), maybeUpdateLongestStreak() added after renderHistory() in importCsv()
- [ ] A11Y-01 acceptance fully satisfied: "all buttons, toggles, preset selectors, and sliders are reachable and activatable via keyboard" (including preset builder Escape path)
- [ ] No new console errors or warnings
- [ ] All six phase 10.1 must-haves verified in codebase

**After completion:** Run `/gsd-complete-milestone v1.1` to archive v1.1 and prepare for ship.
</success_criteria>

<output>
Create `.planning/phases/10.1-close-gap-a11y-01-preset-builder-cancel-event-streak-badge-a/10.1-01-SUMMARY.md` when done

**Summary format:**
- Plan: 10.1-01
- Status: complete
- Gaps closed: A11Y-01 keyboard integration, streak badge stale after import
- Changes: 2 code additions in index.html (cancel listener + streak update calls)
- Tests: Manual behavioral verification (keyboard + import paths)
- Verification: All 6 must-haves asserted
</output>

