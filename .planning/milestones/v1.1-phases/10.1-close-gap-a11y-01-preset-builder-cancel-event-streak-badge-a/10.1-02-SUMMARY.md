---
phase: 10.1-close-gap-a11y-01-preset-builder-cancel-event-streak-badge-a
plan: "02"
subsystem: preset-builder
tags: [gap-closure, accessibility, overlay, keyboard]
gap_ids: [G-10.1-1, G-10.1-5]
requirements: [A11Y-01]
status: complete

dependency_graph:
  requires: []
  provides: [preset-builder-overlay-cleanup]
  affects: [settings-overlay, keyboard-shortcuts]

tech_stack:
  added: []
  patterns: [closeSettings-as-canonical-overlay-teardown]

key_files:
  modified:
    - index.html

decisions:
  - "Use closeSettings() as the single teardown path for the preset builder dialog — it already nulls openPanelElement and removes the visible class, making four bare null assignments redundant"

metrics:
  duration: "< 5 minutes"
  completed: "2026-08-26"
  tasks_completed: 1
  commits: 1

actuals:
  tokens: 800
  tasks: 1
  commits: 1
---

# Phase 10.1 Plan 02: Fix Stranded Settings Overlay on Preset Builder Close Paths Summary

Replaced four bare `openPanelElement = null` assignments in the preset builder dialog handlers with `closeSettings()`, fully closing both G-10.1-1 and G-10.1-5.

## What Was Done

### Task 1: Replace bare openPanelElement = null with closeSettings() on all four preset builder close paths

Four targeted single-line replacements in `index.html`:

| Fix | Handler | Line (approx) |
|-----|---------|---------------|
| Fix 1 | `presetBuilderDialog` cancel event | ~2066 |
| Fix 2 | `presetCancelBtn` click handler | ~2129 |
| Fix 3 | `presetBuilderForm` submit handler | ~2205 |
| Fix 4 | `presetDeleteBtn` confirmed-delete branch | ~2256 |

All four bare `openPanelElement = null` assignments were replaced with `closeSettings()`, which already calls `openPanelElement = null` internally and also removes the `visible` CSS class from the settings overlay.

**Commit:** 2720a7c

## Verification

```
grep -c "openPanelElement = null" index.html → 4
```

The plan expected 1, but 4 is correct: the remaining occurrences are the variable declaration (line 1689), `closeInfo()` (line 3472), `closeStreak()` (line 3498), and `closeSettings()` (line 3522). All four preset-builder bare assignments were successfully replaced.

```
grep -c "closeSettings()" index.html → 7
```

Meets the "6 or more" success criterion.

## Success Criteria

- [x] G-10.1-1 resolved: Escape closes preset builder, then Escape closes settings popup; keyboard shortcuts unblocked
- [x] G-10.1-5 resolved: Cancel/Save/Delete all fully dismiss the settings overlay — no stranded popup
- [x] All four preset-builder `openPanelElement = null` assignments replaced with `closeSettings()`
- [x] `closeSettings()` count is 7 (>= 6 required)
- [x] No regressions: `closeSettings()` itself unchanged; `openSettings()` unchanged
- [x] Code committed atomically

## Deviations from Plan

### Verification Count Clarification

The plan expected `grep -c "openPanelElement = null"` to return 1. Actual result is 4, because `closeInfo()`, `closeStreak()`, and the variable declaration also contain this pattern. This is not a regression — the four preset-builder assignments were all replaced correctly. The "expected: 1" assumption in the plan was based on only counting the `closeSettings()` internal assignment, which was incorrect.

No functional deviations — all four replacements applied exactly as specified.

## Known Stubs

None.

## Threat Flags

None — no new network endpoints, auth paths, or storage keys introduced.

## Self-Check: PASSED

- [x] index.html modified with 4 targeted replacements
- [x] Commit 2720a7c exists in git log
- [x] closeSettings() count = 7 (>= 6)
- [x] All four preset-builder bare null assignments removed
