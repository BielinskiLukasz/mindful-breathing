---
status: resolved
phase: 10.1-close-gap-a11y-01-preset-builder-cancel-event-streak-badge-a
source: [10.1-01-SUMMARY.md]
started: 2026-08-25T21:45:00Z
updated: 2026-08-26T00:00:00Z
---

## Current Test

<!-- OVERWRITE each test - shows where we are -->

## Current Test

[testing complete]

## Tests

### 1. Preset Builder Escape Unblocks Keyboard Shortcuts
expected: Open preset builder dialog, press Escape to close it, then press Space — session starts immediately. R (reset) and F (fullscreen) also work right away. No need to click elsewhere first.
result: issue
reported: "nope. Escape close new preset form and we on the settings popup now. And after that escape dont close that popuop (it work if we open settings and click escape). When I click by mouse on that popup or click tab to select any of fields, and then click escape it still not closing. Escape only close top popup."
severity: major

### 2. Streak Badge Updates After JSON Import
expected: Import a JSON history file (Settings > Import). Immediately after import completes, the streak badge (if on the main screen) reflects the correct current streak based on the imported sessions — no page reload required.
result: pass

### 3. Streak Badge Updates After CSV Import
expected: Import a CSV history file (Settings > Import). Immediately after import completes, the streak badge updates to reflect the correct current streak from the imported data — no page reload required.
result: pass

### 4. Longest Streak Reflects Imported Historical Data
expected: Import history data that contains a longer consecutive streak than the current stored value. After import, the longest streak shown in the streak panel reflects the imported data's longest streak (not the pre-import value).
result: pass

### 5. Other Preset Builder Close Paths Still Work
expected: Open the preset builder. Close it via the Cancel button (not Escape). Keyboard shortcuts (Space, R, F) still work normally afterward. Repeat with Save (create a preset) and delete paths — none should leave keyboard shortcuts blocked.
result: issue
reported: "also there is settings screen after closing preset builder..."
severity: major

## Summary

total: 5
passed: 3
issues: 2
pending: 0
skipped: 0
blocked: 0

## Gaps

- gap_id: G-10.1-1
  truth: "Escape closes preset builder, then subsequent Escape closes the parent settings popup and keyboard shortcuts (Space, R, F) work immediately"
  status: resolved
  reason: "User reported: Escape closes new preset form but settings popup remains open; further Escape presses do not close settings popup even after clicking into it or tabbing to a field; Escape only closes the topmost popup"
  severity: major
  test: 1
  root_cause: "Escape handler (line 2066) sets openPanelElement = null but never calls closeSettings() — the settings overlay's 'visible' CSS class is never removed. The Escape key handler at line 3590 checks openPanelElement === settingsOverlay which evaluates to false (null), so subsequent Escape presses cannot close the stranded settings popup."
  artifacts:
    - path: "index.html"
      issue: "line 2066 (cancel/Escape path): openPanelElement = null without calling closeSettings()"
    - path: "index.html"
      issue: "line 3590 (Escape handler): guard 'openPanelElement === settingsOverlay' fails when openPanelElement is null"
  missing:
    - "Replace openPanelElement = null with closeSettings() on all four preset builder close paths (lines 2066, 2129, 2205, 2256)"
  debug_session: ".planning/debug/preset-builder-settings-popup-stranded.md"

- gap_id: G-10.1-5
  truth: "Closing preset builder via Cancel (or Save, or delete) returns user fully to the main screen — settings popup does not remain open"
  status: resolved
  reason: "User reported: settings screen still visible after closing preset builder via Cancel button — all close paths leave the settings popup stranded"
  severity: major
  test: 5
  root_cause: "Same root as G-10.1-1. Cancel button (line 2129), Save/submit (line 2205), and Delete confirmed (line 2256) all set openPanelElement = null without calling closeSettings() — the settings overlay is never hidden."
  artifacts:
    - path: "index.html"
      issue: "line 2129 (Cancel button): openPanelElement = null without calling closeSettings()"
    - path: "index.html"
      issue: "line 2205 (Save/submit): openPanelElement = null without calling closeSettings()"
    - path: "index.html"
      issue: "line 2256 (Delete confirmed): openPanelElement = null without calling closeSettings()"
  missing:
    - "Replace openPanelElement = null with closeSettings() on all four preset builder close paths (lines 2066, 2129, 2205, 2256)"
  debug_session: ".planning/debug/preset-builder-settings-popup-stranded.md"
