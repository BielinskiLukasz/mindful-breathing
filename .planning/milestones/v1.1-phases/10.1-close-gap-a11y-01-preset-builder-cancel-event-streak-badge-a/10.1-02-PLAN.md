---
phase: 10.1-close-gap-a11y-01-preset-builder-cancel-event-streak-badge-a
plan: 02
type: execute
wave: 1
depends_on: []
gap_closure: true
gap_ids: [G-10.1-1, G-10.1-5]
files_modified:
  - index.html
autonomous: true
requirements: [A11Y-01]

estimate:
  tokens: 12000
  raw_tokens: 6000
  tasks: 1
  confidence: high

must_haves:
  truths:
    - After Escape closes preset builder dialog, the settings overlay has no "visible" class and is hidden
    - After Escape closes preset builder dialog, a second Escape closes the settings popup
    - After Cancel button closes preset builder, the settings overlay is hidden and keyboard shortcuts (Space, R, F) work immediately
    - After Save (submit) closes preset builder, the settings overlay is hidden
    - After Delete confirmed closes preset builder, the settings overlay is hidden
  artifacts:
    - index.html (four openPanelElement = null sites replaced with closeSettings() calls)
  key_links:
    - closeSettings() called on all four preset builder close paths (lines 2066, 2129, 2205, 2256)
    - closeSettings() removes "visible" class from settingsOverlay — the single source of truth for overlay teardown
---

<objective>
Fix the stranded settings overlay bug affecting both G-10.1-1 and G-10.1-5.

Purpose: All four preset builder close paths (Escape/cancel event, Cancel button, Save/submit, Delete confirmed) currently set `openPanelElement = null` without calling `closeSettings()`. This leaves the settings overlay's "visible" CSS class in place, so the overlay stays on screen. The Escape key handler at line 3590 checks `openPanelElement === settingsOverlay`, which evaluates to false when openPanelElement is null, so subsequent Escape presses cannot close the stranded popup. Replacing all four bare `openPanelElement = null` assignments with `closeSettings()` — which already calls `openPanelElement = null` internally — fully resolves both gaps.

Output: index.html with four targeted one-line fixes applied.
</objective>

<execution_context>
@C:/my-code/vibe-coding/mindful-breathing/.claude/gsd-core/workflows/execute-plan.md
@C:/my-code/vibe-coding/mindful-breathing/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace bare openPanelElement = null with closeSettings() on all four preset builder close paths</name>
  <files>index.html</files>
  <action>
    Read index.html and apply four targeted single-line replacements. Each replacement is a standalone Edit call — do not batch them into a single multi-line replacement.

    Fix 1 — cancel event handler (line ~2066):
    Inside the `presetBuilderDialog.addEventListener("cancel", ...)` callback, replace the bare `openPanelElement = null;` with `closeSettings();`. The line immediately follows `deleteBtn.style.color = "";` and immediately precedes `document.getElementById("newPresetBtn")?.focus();`.

    Fix 2 — Cancel button click handler (line ~2129):
    Inside the Cancel button click handler, after `presetBuilderDialog.close();`, replace the bare `openPanelElement = null;` with `closeSettings();`.

    Fix 3 — form submit handler (line ~2205):
    Inside `presetBuilderForm.addEventListener("submit", ...)`, after `presetBuilderDialog.close();`, replace the bare `openPanelElement = null;` with `closeSettings();`. The next line is `saveSettings();` — preserve it.

    Fix 4 — Delete confirmed branch (line ~2256):
    Inside the delete button click handler, after `presetBuilderDialog.close();`, replace the bare `openPanelElement = null;` with `closeSettings();`.

    Do not modify the `closeSettings()` function itself (lines ~3520–3524). Do not modify `openSettings()`. Do not change any other logic. `closeSettings()` already calls `openPanelElement = null` internally, so the four replacement calls are complete and self-contained — no additional null assignment is needed after each `closeSettings()` call.
  </action>
  <verify>
    <automated>grep -c "openPanelElement = null" C:/my-code/vibe-coding/mindful-breathing/index.html</automated>
    Expected output: 1 (only the assignment inside closeSettings() itself remains; all four preset builder bare assignments are gone).
    Additional check: grep -c "closeSettings()" C:/my-code/vibe-coding/mindful-breathing/index.html must return at least 6 (original calls + 4 new ones).
  </verify>
  <done>
    `grep -c "openPanelElement = null" index.html` returns 1.
    `grep -c "closeSettings()" index.html` returns 6 or more.
    In-browser manual check: open preset builder, press Escape — settings overlay disappears, Space starts session. Open preset builder, click Cancel — settings overlay disappears. G-10.1-1 and G-10.1-5 resolved.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user interaction → DOM state | Button clicks and keyboard events mutate overlay visibility |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10.1-02-01 | Tampering | openPanelElement state | low | accept | Pure UI state variable; no authentication or data integrity implications. closeSettings() is already called from trusted event handlers. |
</threat_model>

<verification>
1. `grep -c "openPanelElement = null" index.html` returns exactly 1 (the line inside closeSettings() itself).
2. `grep -c "closeSettings()" index.html` returns 6 or more.
3. Manual browser check: preset builder Escape → settings overlay hidden → Escape again → no popup remains → Space/R/F work.
4. Manual browser check: Cancel button → settings overlay hidden.
5. Manual browser check: Save (create preset) → settings overlay hidden.
6. Manual browser check: Delete preset → settings overlay hidden.
</verification>

<success_criteria>
- G-10.1-1 resolved: Escape closes preset builder, then Escape closes settings popup; keyboard shortcuts unblocked.
- G-10.1-5 resolved: Cancel/Save/Delete all fully dismiss the settings overlay — no stranded popup.
- `openPanelElement = null` appears exactly once in index.html (inside closeSettings()).
- No regressions: `closeSettings()` still works normally when triggered from the settings close button, overlay backdrop click, or Escape while settings is the top panel.
</success_criteria>

<output>
Create `.planning/phases/10.1-close-gap-a11y-01-preset-builder-cancel-event-streak-badge-a/10.1-02-SUMMARY.md` when done.
</output>
