---
phase: 08-accessibility-csv-import
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - index.html
autonomous: true
gap_closure: true
gap_ids: [G-08-3]
requirements: [A11Y-01]
user_setup: []

must_haves:
  truths:
    - "Tab key pressed from any open panel element cycles focus within panel only"
    - "Tab pressed from last focusable element in panel wraps to first element (forward or backward)"
    - "Tab pressed from input field inside panel does not escape to background elements"
  artifacts:
    - path: "index.html"
      description: "keydown handler with focus trap block executing before INPUT/TEXTAREA early return"
  key_links:
    - "Focus trap if-block intercepts Tab before handler returns for INPUT elements"
    - "INPUT/TEXTAREA early return skips global shortcuts but allows panel focus trap"
---

<objective>
Reorder keydown handler if-blocks so Tab focus trap executes before INPUT/TEXTAREA early return, allowing Tab to cycle correctly from input fields within an open panel.

**Gap:** G-08-3 — "Tab key cycles only within open panel; never reaches browser chrome or background buttons"  
**Root Cause:** Focus trap block is unreachable when focus is on INPUT (early return fires first)  
**Fix:** Move focus trap if-block before INPUT/TEXTAREA guard

Purpose: Restore keyboard navigation compliance (A11Y-01) by trapping Tab focus inside panels at all times
Output: Working focus trap that intercepts Tab from both button and input elements
</objective>

<execution_context>
@C:/my-code/vibe-coding/mindful-breathing/.claude/gsd-core/workflows/execute-plan.md
@C:/my-code/vibe-coding/mindful-breathing/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/08-accessibility-csv-import/08-UAT.md (gap G-08-3 root_cause explanation)
</context>

<tasks>

<task type="auto">
  <name>Reorder keydown handler: move focus trap before INPUT/TEXTAREA early return</name>
  <files>index.html</files>
  <action>
In the keydown event listener (starting ~line 2721), reorder the if-blocks:

**Current (broken) order:**
1. Early return for INPUT/TEXTAREA (line 2722)
2. Early return for meta/ctrl/alt (line 2723)
3. Focus trap if-block for Tab (lines 2725–2741)

**New (correct) order:**
1. Early return for meta/ctrl/alt (line 2723) — keep in position
2. Focus trap if-block for Tab (move lines 2725–2741 up) — execute BEFORE INPUT check
3. Early return for INPUT/TEXTAREA (move line 2722 down) — after focus trap

This ensures Tab presses from input elements inside a panel are intercepted by the focus trap before the handler returns, fixing the escape-to-background behavior.

The focus trap block (lines 2725–2741) should remain functionally unchanged — only its position moves up in the handler.
  </action>
  <verify>
    <automated>
Open the app in browser, open Settings panel (click gear icon), focus on any input field (e.g., a duration slider or text input), press Tab repeatedly — focus should cycle only within the Settings panel elements and NOT reach the Start button or other background elements. Check browser console for any JS errors (should be clean).
    </automated>
  </verify>
  <done>
The keydown handler processes Tab interception before evaluating INPUT/TEXTAREA tag names. User can Tab from any open panel element (button or input) and focus cycles only within that panel, never escaping to browser chrome or background buttons. No console errors.
  </done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User Input → Keyboard Handler | Keyboard events originate from the user; handler must correctly prioritize focus management over input filtering |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-03-AC | Tampering | keydown handler control flow | low | mitigate | Reorder if-blocks to ensure focus trap executes before INPUT guard; verify Tab no longer escapes panels |

</threat_model>

<verification>

1. **Manual verification:** Open Settings or Info panel, focus on an input field (slider or text), press Tab — focus should stay within the panel
2. **No regressions:** Space/R/F shortcuts still disabled when panel is open (existing logic unaffected)
3. **Console clean:** No JS errors during Tab cycling
4. **Backward compat:** Non-panel keyboard interactions (global shortcuts, ESC close) unchanged

</verification>

<success_criteria>

- Gap G-08-3 resolved: Tab key pressed from inside an open panel (including from input elements) cycles focus only within that panel
- Focus trap logic is reachable from both button and input elements
- No console errors or side effects to other keyboard shortcuts
- Test 3 in 08-UAT.md passes on re-run: "Focus trapped inside open panel — returns to opener on close"

</success_criteria>

<output>
Create `.planning/phases/08-accessibility-csv-import/08-03-SUMMARY.md` when done, documenting:
- Files modified: index.html
- Lines changed: keydown handler (lines ~2721–2764)
- Verification: Tab focus trap confirmed working from input elements
- Gap G-08-3 closed ✓
</output>
