---
phase: 02-theming-ux-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [index.html]
autonomous: true
requirements: [THEME-01, THEME-02]
must_haves:
  truths:
    - "User can toggle theme between light and dark by clicking icon button in settings"
    - "Theme toggle updates all UI colors instantly and smoothly (220ms fade)"
    - "SVG ring stroke color adapts to theme automatically"
    - "Theme preference persists when user closes and reopens the app"
    - "Theme toggle works correctly even during active breathing session"
  artifacts:
    - path: "index.html"
      provides: "CSS custom properties for light/dark themes, theme toggle button, toggle handler"
      min_lines: 2100
  key_links:
    - from: "CSS :root custom properties"
      to: "SVG ring stroke color"
      via: "var(--accent) inheritance"
      pattern: "stroke: var\\(--accent\\)"
    - from: "theme toggle button click"
      to: "document.documentElement.style.setProperty"
      via: "toggleTheme() handler"
      pattern: "toggleTheme.*setProperty"
    - from: "saveSettings() call"
      to: "localStorage STORAGE_KEY"
      via: "themePreference key"
      pattern: "themePreference.*dark.*light"

---

<objective>
Implement light/dark theme toggle with CSS custom property updates, smooth 220ms transitions, and persistent localStorage storage. This unlocks THEME-01 (toggle functionality) and THEME-02 (persistence).

Purpose: Users gain control over visual appearance and app adapts instantly to preference across sessions.

Output: Theme toggle button in settings panel (☀️/🌙 icons), CSS custom property definitions for light theme, theme handler wired to settings, and localStorage persistence.
</objective>

<execution_context>
@C:\Users\lukasz.bielinski\projects\mindful-breathing\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\lukasz.bielinski\projects\mindful-breathing\.planning\phases\02-theming-ux-polish\02-CONTEXT.md
@C:\Users\lukasz.bielinski\projects\mindful-breathing\.planning\phases\02-theming-ux-polish\02-RESEARCH.md
@C:\Users\lukasz.bielinski\projects\mindful-breathing\.planning\phases\01-session-history-vibration\01-01-SUMMARY.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md

Phase 1 established single-file architecture, localStorage patterns (saveSettings/loadSettings), and existing CSS custom property system (--bg, --accent, --text). Phase 2 leverages this foundation to add light theme values and toggle handler.

CONTEXT.md locks: CSS custom properties mechanism, dark-by-default, no prefers-color-scheme auto-detect, 220ms smooth transition, icon-only toggle (☀️/🌙) in settings panel, warm color palette for light theme.

RESEARCH.md validates: CSS `clamp()` ready for font sizing (Phase 2 Plan 2), SVG stroke color responds instantly to custom property changes, light theme palette meets WCAG AA contrast (4.5:1 minimum), localStorage quota negligible for theme key.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define Light Theme CSS Custom Properties</name>
  <files>index.html</files>
  <action>
Add light theme color definitions to the CSS :root section (after existing dark theme custom properties, ~line 1080). Create two sets of values:

1. Define light theme colors as separate CSS variables:
   - Light background: #f5f1ed (cream/off-white)
   - Light primary text: #5c4033 (warm dark brown)
   - Light secondary text: #a89e8c (warm gray-beige)
   - Light accent (ring color): #d4a574 (warm gold/tan)
   - Light accent soft (for UI elements): rgba(212,165,116,0.18)
   - Light card background: #ebe7e1 (slightly darker cream)

2. Verify all light theme text colors meet WCAG AA contrast (4.5:1) against light background using WebAIM Contrast Checker during implementation. Use the exact hex values above; do not substitute.

3. Keep existing dark theme variables unchanged (--bg: #111, --accent: #9aa0a6, etc.); light theme values are separate definitions that will be swapped via JavaScript.

Note: Per D-15 (CSS custom properties mechanism), do NOT use class-based switching (.light-theme class). Instead, define all light values and swap them via document.documentElement.style.setProperty() in the toggle handler.

Implementation should be CSS-only in this task; JavaScript integration happens in Task 2.
  </action>
  <verify>
    <automated>grep -c "f5f1ed\|5c4033\|a89e8c\|d4a574" index.html</automated>
  </verify>
  <done>Light theme hex colors defined in CSS :root section. All 6 color variables present (bg, text, textSoft, accent, accentSoft, cardBg or equivalent names). No functional changes yet; toggle handler not yet wired.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Theme Toggle Handler and Settings Panel Integration</name>
  <files>index.html</files>
  <action>
Implement the toggleTheme() function in the JavaScript section (within CONTROLS area, after existing start/stop/reset handlers, ~line 1700). Function must:

1. Read current theme state from localStorage[STORAGE_KEY].isDarkMode boolean
2. Determine new color set based on current state (if dark, switch to light; if light, switch to dark)
3. Apply all light OR dark custom properties via document.documentElement.style.setProperty() for: --bg, --accent, --text, --textSoft, --accentSoft, --cardBg (or equivalent properties currently in use)
4. Update localStorage STORAGE_KEY with flipped isDarkMode value
5. Update toggle button visual state (set button text to ☀️ if now in light theme, 🌙 if now in dark theme)
6. No console output; silent failure if localStorage unavailable (try/catch pattern per Phase 1)

Per CONTEXT.md decision D-17 (smooth 220ms transition), CSS transition is already applied to color properties (ensure existing CSS has `transition: background-color 220ms ease, color 220ms ease` on relevant elements). Toggle function does not need to add transitions; CSS handles animation.

Add theme toggle button to HTML settings panel (near sound/vibration toggle buttons, ~line 500 in HTML). Button structure:
   - ID: themeToggleBtn
   - Content: ☀️ (sun icon for light theme indication)
   - Class: same styling as soundToggle/vibeToggle buttons
   - No aria-label (deferred to Phase 3 per CONTEXT.md)

Wire button click event: `document.getElementById('themeToggleBtn').addEventListener('click', toggleTheme);` in init block.

Ensure toggleTheme() also calls saveSettings() to persist preference to localStorage (can reuse existing saveSettings function or inline localStorage write within toggleTheme).
  </action>
  <verify>
    <automated>grep -c "function toggleTheme\|themeToggleBtn\|addEventListener.*toggleTheme" index.html && grep "isDarkMode" index.html | wc -l</automated>
  </verify>
  <done>toggleTheme() function exists and is wired to #themeToggleBtn click event. Function reads isDarkMode from localStorage, applies setProperty() calls for all color variables, updates button visual state (☀️/🌙), and persists preference. SVG ring color updates via --accent custom property (no additional JavaScript required).</done>
</task>

<task type="auto">
  <name>Task 3: Extend Settings Persistence (saveSettings/loadSettings)</name>
  <files>index.html</files>
  <action>
Extend the existing saveSettings() and loadSettings() functions to include themePreference persistence (in PERSISTENCE section, ~line 1315-1360).

In saveSettings():
   - Capture current isDarkMode state from localStorage read (or from a global state variable if you choose to track it)
   - Include isDarkMode in the JSON object saved to STORAGE_KEY
   - Maintain backward compatibility: if old sessions lack isDarkMode, default to true (dark theme)

In loadSettings():
   - Read isDarkMode from STORAGE_KEY after JSON.parse
   - If isDarkMode is undefined or missing, default to true
   - Store in a global variable if tracking locally, or re-apply CSS custom properties immediately on load
   - Apply CSS custom properties for the loaded theme (if isDarkMode true, apply dark colors; if false, apply light colors)

Call loadSettings() in app initialization block (already exists, ~line 1905). After loadSettings completes, ensure CSS custom properties are applied for the loaded theme state before any rendering occurs.

Update toggle button visual state in loadSettings() to match loaded theme (☀️ if light, 🌙 if dark).

Test localStorage schema: existing sessions may not have isDarkMode field. Ensure no errors on parse if field missing; always provide fallback.
  </action>
  <verify>
    <automated>grep -c "isDarkMode" index.html && grep "JSON.parse.*STORAGE_KEY" index.html | grep -c "isDarkMode"</automated>
  </verify>
  <done>saveSettings() includes isDarkMode in persisted JSON. loadSettings() reads isDarkMode with default fallback (true = dark), and applies CSS custom properties on load. App initializes with correct theme colors before first render. Backward compatibility verified: missing isDarkMode field defaults safely to dark theme.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
CSS light theme color variables, theme toggle button in settings panel, toggleTheme() handler wired to localStorage persistence, smooth 220ms color transitions.
  </what-built>
  <how-to-verify>
1. Open the app in a browser (localhost:8080 or GitHub Pages)
2. Locate the theme toggle button in settings (should be ☀️ or 🌙 icon button, below sound/vibration toggles)
3. Verify current theme is dark (dark background, light text, gray accent ring)
4. Click theme toggle button once
5. Visually inspect: all UI colors should transition smoothly (220ms fade) to light theme
   - Background should change to light cream (#f5f1ed)
   - Text should change to warm brown (#5c4033)
   - Ring SVG stroke should change to warm gold (#d4a574)
   - Button icon should toggle to opposite icon (☀️ → 🌙 or vice versa)
6. Confirm no harsh flashing or jumping; transition should feel smooth
7. Close the browser tab completely
8. Reopen the app; theme preference should persist (light theme still active if you toggled to light)
9. Toggle back to dark theme; close and reopen; confirm dark theme persists
10. Test toggle during active breathing session:
    - Start a breathing session (click Start)
    - Click theme toggle button while ring is animating
    - Verify ring stroke color changes smoothly without jitter or lag
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass, or describe any issues (colors wrong, no transition, toggle doesn't persist, etc.)</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| localStorage ↔ App State | User controls theme preference; stored in unencrypted localStorage |
| CSS Custom Properties ↔ DOM | Color values propagate to all UI elements and SVG via cascading CSS |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | localStorage themePreference | accept | Theme preference contains only "light" or "dark" string; no sensitive data. localStorage unencrypted but acceptable for UI preference. |
| T-02-02 | Denial of Service | toggleTheme() handler | accept | Handler is synchronous and completes in <5ms; no performance risk. CSS transitions are hardware-accelerated. |
| T-02-03 | Information Disclosure | CSS color values | accept | All hex color values are visible in CSS source; no secrets exposed. Light/dark theme preference is user-visible state. |

No mitigations required. Phase 2 adds no new attack surface (no network requests, no user input beyond button click, no authentication).
</threat_model>

<verification>
## Automated Checks

After executing all tasks:

1. Light theme colors are defined:
   ```bash
   grep -c "#f5f1ed\|#5c4033\|#a89e8c\|#d4a574" index.html
   # Expected: ≥6 matches (each color appears in :root section + potentially in comments/examples)
   ```

2. toggleTheme function exists and is wired:
   ```bash
   grep "function toggleTheme\|toggleTheme()" index.html | wc -l
   # Expected: ≥2 (function definition + event listener call)
   ```

3. localStorage persistence includes isDarkMode:
   ```bash
   grep "isDarkMode" index.html | wc -l
   # Expected: ≥4 (saveSettings, loadSettings, toggleTheme, init)
   ```

4. SVG ring uses --accent custom property:
   ```bash
   grep "stroke: var(--accent)" index.html
   # Expected: ≥1 match in SVG ring element
   ```

5. CSS transition applied (220ms fade):
   ```bash
   grep "220ms\|transition.*color" index.html | head -5
   # Expected: ≥1 match showing 220ms transition on color properties
   ```

## Manual Verification

1. Visual: Theme toggle produces smooth 220ms color fade (no flashing)
2. Persistence: After toggle → close tab → reopen → theme persists
3. SVG: Ring stroke color adapts when theme changes
4. Active session: Toggle during breathing animation; no lag or jitter
5. Both themes functional: All buttons, text, controls visible in both light and dark

## Success Criteria

- [x] Light theme CSS variables defined (6 colors: bg, text, textSoft, accent, accentSoft, cardBg)
- [x] Theme toggle button rendered in settings panel with icon (☀️/🌙)
- [x] toggleTheme() function wired to button click
- [x] CSS custom properties swapped via setProperty() (all 6 colors updated on toggle)
- [x] 220ms smooth transition visible on all color changes
- [x] Theme preference saved to localStorage under STORAGE_KEY.isDarkMode
- [x] loadSettings() applies theme colors on app startup
- [x] Theme can be toggled during active breathing session without jitter
- [x] Theme persists across browser sessions (close/reopen app)
- [x] Light theme colors meet WCAG AA contrast (4.5:1) — verified via WebAIM Contrast Checker
</verification>

<output>
Create `.planning/phases/02-theming-ux-polish/02-01-SUMMARY.md` after execution.

Commit message format:
```
feat(02-01): implement light/dark theme toggle with persistent storage

- Add light theme CSS custom properties (#f5f1ed background, #5c4033 text, #d4a574 accent)
- Implement toggleTheme() handler wired to settings panel button (☀️/🌙 icons)
- Extend saveSettings/loadSettings to persist isDarkMode to localStorage
- Apply smooth 220ms CSS transitions on theme swap
- SVG ring stroke color adapts automatically via --accent custom property

Per D-15 (CSS custom properties), D-16 (dark-by-default), D-17 (220ms smooth transition).
Implements THEME-01 (toggle) and THEME-02 (persistence).
```
</output>

---

## Phase 2 Plan 2: Responsive Typography (Separate Plan)

**Objective:** Replace fixed font sizes with CSS `clamp()` formulas for fluid scaling across 320px to 4K widths.

**Requirements:** UX-01 (font sizes readable across all devices)

**Scope:** 
- Body/labels: `clamp(12px, 1.5vw + 8px, 16px)`
- Headings (phase, cycle): `clamp(18px, 3vw + 12px, 28px)`
- Countdown timer (focal point): `clamp(40px, 8vw + 20px, 64px)`
- Buttons/controls: `clamp(13px, 1.75vw + 9px, 17px)`
- History card text: `clamp(11px, 1.2vw + 7px, 14px)`
- Line-height & letter-spacing: Scale proportionally with font sizes
- Delete existing fixed font-size media queries that `clamp()` replaces

**Status:** Ready for execution (Plan 2, Wave 1)

---

## Phase 2 Plan 3: Landscape Layout (Separate Plan)

**Objective:** Add side-by-side layout (ring 50% left, controls/history 50% right) for landscape orientation.

**Requirements:** UX-02 (landscape displays optimally)

**Scope:**
- Add `@media (orientation: landscape)` or width-based breakpoint
- Create flex wrapper container for 50/50 split
- Ring section (left): scales with `clamp()` to fill vertical space
- Controls section (right): flexbox column with history scrollable, controls fixed at bottom
- Cap max-width at ~1400px for 4K, centered with padding
- Test on mobile landscape (480px), tablet landscape (768px+), desktop landscape (1920px)

**Status:** Ready for execution (Plan 3, Wave 1)

---

## Phase 2 Plan 4: Cross-Device Testing (Verification Task)

**Objective:** Manual visual testing on 320px to 4K widths, both portrait and landscape.

**Requirements:** UX-01, UX-02, UX-03 (readable fonts, optimal layout, visual hierarchy)

**Scope:**
- Test devices: iPhone 12 (390px), iPad (768px), desktop (1920px), 4K (2560px+)
- Verify: No horizontal scroll, fonts readable, theme toggle smooth, ring/controls alignment
- Platforms: Chrome, Firefox, Safari (if available)

**Status:** Ready for execution (Plan 4, Wave 2, depends on Plans 2 & 3)

---
