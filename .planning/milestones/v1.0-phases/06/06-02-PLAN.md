---
phase: 06-bug-fixes-visual-polish
plan: 02
type: execute
wave: 2
depends_on:
  - "06-01"
files_modified:
  - index.html
autonomous: true
requirements:
  - TRACK-01
must_haves:
  truths:
    - "Clicking Stop after completing at least 1 full breathing cycle saves an entry with incomplete: true in localStorage HISTORY_KEY (D-01, D-02)"
    - "Clicking Stop with zero completed cycles (cycleCount === 1) does NOT create an incomplete entry (D-01)"
    - "Clicking the cancel/stop button during the 3-second pre-start countdown does NOT create an incomplete entry (D-04)"
    - "Reset button does NOT save an incomplete entry (D-02)"
    - "Incomplete entries render in the history list at opacity 0.6 with an italic 'Incomplete' suffix on the same line (D-06, D-07)"
    - "Complete entries (existing and new) render with no opacity change and no Incomplete label (backward compatible — D-05)"
    - "Incomplete entries are sorted chronologically inline with complete entries (D-08)"
    - "The preset color dot is unchanged for incomplete entries (D-09)"
  artifacts:
    - "index.html toggleBtn click handler: if (isRunning) branch includes saveHistory call with incomplete: true when cycleCount >= 2 (per D-01, D-02)"
    - "index.html renderHistory(): map callback checks s.incomplete flag, applies opacity:0.6 and italic Incomplete label conditionally (per D-06, D-07)"
  key_links:
    - "toggleBtn click handler guard: if (isCountingDown) { cancelCountdown(); return; } precedes if (isRunning) branch — ensures D-04 is satisfied without extra flag"
    - "stop() inside toggleBtn updates totalSessionMs before returning — incomplete save reads totalSessionMs for durationMs (D-03)"
    - "finishSession() calls stop() directly (not via click handler) then saveHistory for complete session — incomplete save code in click handler does NOT fire for completed sessions"
---

<objective>
Implement incomplete session tracking in index.html: save sessions that were stopped mid-way (after completing at least 1 cycle) with an `incomplete: true` flag, and display them in the history list with muted opacity and an italic "Incomplete" suffix (TRACK-01, decisions D-01 through D-09).

Purpose: Users who stop a session early can still see their partial progress in the history, with clear visual distinction from completed sessions.

Output: Modified index.html where: (a) the Stop button path in toggleBtn click handler saves eligible incomplete sessions, and (b) renderHistory() renders the incomplete flag with correct styling. Backward compatible with existing history entries.
</objective>

<execution_context>
@C:/Users/lukasz.bielinski/OneDrive - Accenture/my-folders/my-projects/vibe-coding/mindful-breathing/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/lukasz.bielinski/OneDrive - Accenture/my-folders/my-projects/vibe-coding/mindful-breathing/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06/06-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Save incomplete sessions from Stop button click (TRACK-01, D-01 through D-04)</name>
  <files>index.html</files>
  <read_first>
    - index.html lines 2178–2198 (toggleBtn click handler — the if (isRunning) branch to modify)
    - index.html lines 2116–2135 (stop() function — understand what it does and what it updates)
    - index.html lines 1954–1962 (finishSession() — confirm it calls stop() directly, not via handler)
    - index.html lines 1593–1610 (saveHistory() — understand the entry schema and silent catch)
    - index.html lines 1337–1342 (STATE — cycleCount initial value and semantics)
    - .planning/phases/06/06-CONTEXT.md decisions D-01 through D-04
  </read_first>
  <action>
    Understand the cycleCount semantics before editing:
    - cycleCount is initialized to 1 (not 0). It represents the CURRENT cycle in progress.
    - It is incremented to 2 only after the first full cycle is completed (all phases of cycle 1 done).
    - Therefore: cycleCount >= 2 means at least 1 full cycle has been completed. (D-01)

    Locate the toggleBtn click handler (around line 2178). The relevant branch reads:

      if (isRunning) { stop(); return; }

    Replace that single line with an expanded block:

      if (isRunning) {
        stop();
        // Save incomplete session if at least 1 full cycle completed (D-01) via Stop button only (D-02).
        // D-04 is satisfied: if isCountingDown was true, the handler already returned at the earlier
        // `if (isCountingDown) { cancelCountdown(); return; }` guard — so this branch is only reached
        // when a session is actively running (not during countdown).
        // stop() updates totalSessionMs before returning — use it for durationMs (D-03).
        if (cycleCount >= 2) {
          saveHistory({
            date: new Date().toISOString(),
            durationMs: totalSessionMs,
            cycles: cycleCount - 1,
            preset: activePresetKey,
            incomplete: true
          });
          renderHistory();
        }
        return;
      }

    Key points about this implementation:
    - D-04 (countdown guard): the click handler's preceding `if (isCountingDown) { cancelCountdown(); return; }`
      ensures this branch is unreachable when the countdown is active. No additional isCountingDown check
      is needed inside this block.
    - D-02 (Stop button only): this code lives in the toggleBtn click handler, NOT in stop() or reset().
      reset() calls stop() separately and will not trigger this save. finishSession() calls stop() directly
      (not via the click handler) and then calls its own saveHistory() for the complete session.
    - D-03 (durationMs = elapsed at stop): stop() calls `totalSessionMs += Date.now() - sessionResumeTs`
      internally before returning. After stop() returns, totalSessionMs is the correct total elapsed time.
    - D-01 (threshold): `cycleCount >= 2` means completedCycles >= 1.
    - `cycles: cycleCount - 1` stores the number of COMPLETED (not in-progress) cycles.
    - The saveHistory() call uses the existing silent try/catch pattern already in saveHistory().
  </action>
  <verify>
    Manual test sequence (requires browser):
    1. Open index.html, start a session (Relax preset, default 5-cycle goal)
    2. Let 1 full cycle complete (all 4 phases: Inhale → Hold → Exhale → Hold)
    3. Click Stop — open browser DevTools → Application → localStorage → mb_history
    4. Confirm: the most recent entry has `"incomplete": true`, `"cycles": 1`, and a valid `"durationMs"`
    5. Reset and start again; click Stop immediately (before any phase completes, cycleCount = 1)
    6. Confirm: NO new entry with `incomplete: true` was added to mb_history
    7. Start, immediately click the Stop button during the 3-second countdown
    8. Confirm: the toggleBtn handler's `if (isCountingDown)` guard fires — NO incomplete entry added

    <automated>grep -c "incomplete: true" index.html</automated>
    Expected: 1 (only the saveHistory call in the click handler)
  </verify>
  <acceptance_criteria>
    - Source: the toggleBtn click handler contains `saveHistory({` with `incomplete: true` inside the
      `if (isRunning)` branch, conditioned on `cycleCount >= 2`
    - Source: the saveHistory call in the toggleBtn handler passes `cycles: cycleCount - 1`
    - Source: the saveHistory call passes `durationMs: totalSessionMs`
    - Source: no new incomplete save logic exists in the `stop()` function or `reset()` function
    - Behavioral: stopping after 1+ complete cycles → localStorage entry with `incomplete: true` exists
    - Behavioral: stopping with 0 complete cycles (cycleCount = 1) → no incomplete entry in localStorage
    - Behavioral: cancelling the countdown (Stop during 3s countdown) → no incomplete entry
    - Behavioral: completing a full session (all cycles done) → finishSession fires, history entry has no
      `incomplete` field (complete session uses existing code path)
  </acceptance_criteria>
  <done>
    Stop button saves incomplete sessions with `incomplete: true` when cycleCount >= 2. Reset, countdown
    cancel, and session completion are unaffected. saveHistory() is called with the correct schema extension.
  </done>
</task>

<task type="auto">
  <name>Task 2: Render incomplete entries with muted opacity and italic label (TRACK-01, D-05 through D-09)</name>
  <files>index.html</files>
  <read_first>
    - index.html lines 1612–1668 (renderHistory() function — full function including the map template)
    - index.html lines 1639–1650 (the inner map template — specific lines to modify)
    - .planning/phases/06/06-CONTEXT.md decisions D-05 through D-09
  </read_first>
  <action>
    Locate the `renderHistory()` function (around line 1612). Inside its try block, find the `.map(s => { ... })` call (around lines 1639–1650) that produces the historyItem HTML.

    The current map callback returns:

      return `&lt;div class="historyItem"&gt;
        &lt;span&gt;&lt;span style="color: ${presetColor};"&gt;&amp;#9679;&lt;/span&gt; ${presetName} &amp;bull; ${formatDuration(s.durationMs)} &amp;bull; ${s.cycles} cycle${s.cycles !== 1 ? "s" : ""} &amp;bull; ${dateLabel}&lt;/span&gt;
      &lt;/div&gt;`;

    Add two local variables before the return statement, then modify the template string to use them.
    Insert just before the existing `return` line:

      const rowStyle = s.incomplete ? ' style="opacity:0.6"' : '';
      const incompleteLabel = s.incomplete ? ' &amp;bull; &lt;em&gt;Incomplete&lt;/em&gt;' : '';

    Then update the returned template to include both:

      return `&lt;div class="historyItem"${rowStyle}&gt;
        &lt;span&gt;&lt;span style="color: ${presetColor};"&gt;&amp;#9679;&lt;/span&gt; ${presetName} &amp;bull; ${formatDuration(s.durationMs)} &amp;bull; ${s.cycles} cycle${s.cycles !== 1 ? "s" : ""} &amp;bull; ${dateLabel}${incompleteLabel}&lt;/span&gt;
      &lt;/div&gt;`;

    Design decisions encoded:
    - D-05: `s.incomplete === true` check — entries without the field (undefined) are falsy, treated as complete (backward compatible)
    - D-06: `opacity:0.6` as inline style on the full historyItem div row
    - D-07: ` &bull; &lt;em&gt;Incomplete&lt;/em&gt;` appended inline after the date label
    - D-08: no sort change — history renders in insertion order (chronological inline) unchanged
    - D-09: preset color dot uses `presetColor` unchanged — no modification to the dot span

    Do NOT change the existing `presetColor`, `formatDuration`, `dateLabel`, `presetName`, or any
    other template variable. Only add `rowStyle` and `incompleteLabel` variables and splice them
    into the existing template in the two locations shown above.
  </action>
  <verify>
    Manual test (build on test from Task 1):
    1. Open index.html, run a session with 2+ cycles, click Stop (creates an incomplete entry)
    2. Open the history section — the newest entry should appear at reduced opacity (visually muted)
    3. The entry text should read: "● [Preset] • [duration] • [N] cycle(s) • Today • Incomplete"
       where "Incomplete" is in italic
    4. Run a complete session (all cycles done) — that entry should appear at full opacity with no
       "Incomplete" suffix
    5. Both entries interleave chronologically in the list without grouping

    <automated>grep -c "s.incomplete" index.html</automated>
    Expected: 2 (rowStyle assignment and incompleteLabel assignment)

    <automated>grep -c "Incomplete" index.html</automated>
    Expected: 1 (the incompleteLabel template literal in renderHistory)
  </verify>
  <acceptance_criteria>
    - Source: renderHistory() map callback contains `const rowStyle = s.incomplete ? ' style="opacity:0.6"' : '';`
    - Source: renderHistory() map callback contains `const incompleteLabel = s.incomplete ?` referencing `&lt;em&gt;` tag
    - Source: the historyItem div template uses `${rowStyle}` for the style attribute
    - Source: the span text uses `${incompleteLabel}` after the dateLabel interpolation
    - Source: no change to the preset dot span or any other existing template variable
    - Behavioral: localStorage entry with `incomplete: true` → rendered at opacity 0.6 with italic "Incomplete" suffix
    - Behavioral: localStorage entry without `incomplete` field (or `incomplete: false`) → rendered at full opacity with no suffix (backward compatible)
    - Behavioral: preset color dot shows the same color for incomplete entries as for complete entries
    - Behavioral: chronological order of entries is unchanged
  </acceptance_criteria>
  <done>
    Incomplete history entries display at 0.6 opacity with an inline italic "Incomplete" suffix. Complete entries are visually unchanged. Backward compatible with existing entries that lack the incomplete field.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| localStorage → renderHistory() | Session entries (including the new incomplete: true field) are read from localStorage and interpolated into innerHTML — existing XSS surface, no new fields added to the interpolation beyond the boolean flag |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation |
|-----------|----------|-----------|----------|-------------|------------|
| T-06-03 | Tampering | localStorage incomplete flag | low | accept | Single-user local app. An attacker with localStorage access can already modify all session data. The incomplete flag is boolean — falsy values fall back to "complete" rendering safely. |
| T-06-04 | Information Disclosure | Session durationMs in incomplete entries | low | accept | Duration is already stored for complete sessions. Incomplete entries expose no additional sensitive information. |
</threat_model>

<verification>
Full TRACK-01 verification after both tasks complete:

Scenario A (eligible incomplete — should save and display):
1. Open index.html, start Relax session
2. Wait for cycle 1 to finish completely (all 4 phases: Inhale/Hold/Exhale/Hold)
3. Click Stop (cycleCount is now 2)
4. Open history: newest entry reads "● Relax • [time] • 1 cycle • Today • Incomplete" at 0.6 opacity
5. localStorage mb_history[0] has `incomplete: true`, `cycles: 1`, valid `durationMs`

Scenario B (ineligible — 0 cycles completed):
1. Start a session, click Stop immediately (before first phase completes, cycleCount = 1)
2. History is unchanged — no new incomplete entry

Scenario C (countdown cancel — D-04):
1. Click Start — during the 3s countdown, click Stop
2. History is unchanged — no entry added

Scenario D (complete session — existing behavior unchanged):
1. Run a full session to completion
2. History shows the complete entry at full opacity with no "Incomplete" suffix

Scenario E (backward compatibility — D-05):
1. Pre-existing localStorage entries (no incomplete field) render at full opacity with no suffix
</verification>

<success_criteria>
- `grep -c "incomplete: true" index.html` returns 1 (toggleBtn handler only)
- `grep -c "s.incomplete" index.html` returns 2 (rowStyle and incompleteLabel in renderHistory)
- Stop after 1+ cycles → localStorage entry has `{ incomplete: true, cycles: N-1, durationMs: T }`
- Stop at 0 cycles → no incomplete entry in localStorage
- Incomplete entries in history UI: opacity 0.6, italic "Incomplete" suffix on the same line
- Complete entries in history UI: unchanged appearance
- Existing localStorage entries without `incomplete` field render as complete (backward compatible)
</success_criteria>

## Artifacts this phase produces

**Modified JS (toggleBtn click handler, ~line 2180):**
- `if (isRunning)` branch expanded to call saveHistory with `{ incomplete: true, durationMs: totalSessionMs, cycles: cycleCount - 1, preset: activePresetKey }` when `cycleCount >= 2`
- renderHistory() called after save to refresh the history display

**Modified JS (renderHistory() map callback, ~line 1639):**
- New local variables: `rowStyle` (string — `' style="opacity:0.6"'` or `''`) and `incompleteLabel` (string — `' &bull; <em>Incomplete</em>'` or `''`)
- historyItem div template uses `${rowStyle}` as inline style attribute
- Span text template appends `${incompleteLabel}` after dateLabel

**No new CSS rules** (opacity applied as inline style per D-06)

**Schema extension (backward compatible):**
- Session entry gains optional field `incomplete: boolean` — absent means complete (per D-05)

<output>
Create `.planning/phases/06/06-02-SUMMARY.md` when done.
</output>
