---
phase: 03-critical-bugs-ux-polish
plan: 08
type: execute
wave: 8
depends_on:
  - "03-07"
files_modified:
  - "index.html"
  - ".planning/phases/03-critical-bugs-ux-polish/VIBRATION-TEST-REPORT.md"
autonomous: false
requirements:
  - TEST-01
gap_closure: true

must_haves:
  truths:
    - "All vibration calls (phase transitions and countdown pulses) go through the vibrate() wrapper for consistent guard logic"
    - "Countdown vibration uses array pattern [40] matching the VIBRATION.pattern = [50] convention (array not plain number)"
    - "Android Chrome DevTools test result is documented: either vibration works after fix OR Samsung/OS restriction is recorded as known limitation"
  artifacts:
    - path: "index.html"
      provides: "showCountdown uses vibrate([40]) wrapper call instead of inline navigator.vibrate(40)"
      contains: "vibrate([40])"
    - path: ".planning/phases/03-critical-bugs-ux-polish/VIBRATION-TEST-REPORT.md"
      provides: "Updated report with DevTools console test result (navigator.vibrate(50) direct test) and conclusion"
  key_links:
    - from: "showCountdown forEach callback"
      to: "vibrate() wrapper"
      via: "vibrate([40]) replaces inline navigator.vibrate(40)"
      pattern: "vibrate\\(\\[40\\]\\)"
---

<objective>
Close verification gap 1 from 03-VERIFICATION.md: normalize countdown vibration to use the vibrate() wrapper (removes inline navigator.vibrate call), then conduct an Android Chrome DevTools diagnostic to determine whether the Vibration API actually works on the test device, and update the test report with findings.

Purpose: The countdown currently bypasses the vibrate() wrapper and calls navigator.vibrate(40) directly. This is inconsistent and harder to maintain. The Android failure root cause is unknown — it could be a Samsung/Android OS restriction, a Chrome policy change, or our call being incorrect. A DevTools console test on the device will isolate the issue.

Output: (1) Normalized vibrate() call in showCountdown; (2) Updated VIBRATION-TEST-REPORT.md with DevTools diagnostic result and conclusion.
</objective>

<execution_context>
@C:/Users/lukasz.bielinski/projects/mindful-breathing/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/lukasz.bielinski/projects/mindful-breathing/.claude/get-shit-done/templates/summary.md
@C:/Users/lukasz.bielinski/projects/mindful-breathing/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/phases/03-critical-bugs-ux-polish/03-CONTEXT.md
@.planning/phases/03-critical-bugs-ux-polish/03-VERIFICATION.md
@.planning/phases/03-critical-bugs-ux-polish/VIBRATION-TEST-REPORT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Normalize countdown vibration to use vibrate() wrapper</name>
  <files>index.html</files>
  <read_first>index.html</read_first>
  <action>
In showCountdown() (around line 1873), find the inline vibration block inside the `[3, 2, 1].forEach` setTimeout callback:
  ```js
  if (vibeEnabled && "vibrate" in navigator) {
    try { navigator.vibrate(40); } catch (_) {}
  }
  ```

Replace it entirely with a single call using the existing vibrate() wrapper:
  ```js
  vibrate([40]);
  ```

Rationale:
- `vibrate()` already guards vibeEnabled and "vibrate" in navigator — the inline guards are redundant
- Using array form `[40]` matches VIBRATION.pattern = [50] convention (per W3C spec, both numbers and arrays work; arrays are preferred for multi-pulse patterns and may behave more predictably on some Android versions)
- This makes all vibration calls in the file consistent with one centralized function

Do NOT change anything else in showCountdown(). The playBeep(350) call added in 03-07 should remain.
  </action>
  <verify>
    - Grep index.html for "navigator.vibrate(40)" — should return NO matches (inline call removed)
    - Grep index.html for "vibrate([40])" — should return 1 match inside showCountdown forEach callback
  </verify>
  <acceptance_criteria>
    - index.html no longer contains `navigator.vibrate(40)` (the inline direct call is gone)
    - index.html contains `vibrate([40]);` inside the showCountdown forEach callback
    - The vibrate() wrapper function at line ~1667 is unchanged
  </acceptance_criteria>
  <done>Countdown vibration uses vibrate() wrapper with array pattern; consistent with phase-transition vibration calls.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Normalized vibration calls in showCountdown() — countdown now uses vibrate([40]) wrapper.

**Android Chrome DevTools diagnostic:** To find the root cause of the vibration failure on Samsung Galaxy A33 5G (Android 16), run a direct API test using Chrome remote debugging.
  </what-built>
  <how-to-verify>
**Setup (desktop + Android device):**
1. Connect Samsung Galaxy A33 5G to your desktop via USB
2. On the Android device: Settings → Developer Options → enable USB Debugging
3. On the Android device: open Chrome, navigate to https://bielinskilukasz.github.io/mindful-breathing/
4. On desktop Chrome: navigate to `chrome://inspect/#devices`
5. Find your device listed; click **Inspect** next to the page tab
6. A DevTools window opens with remote console access to the Android Chrome tab

**Test A — Direct API test (most important):**
In the Console tab of the remote DevTools window, type:
```js
navigator.vibrate(50)
```
Press Enter. Does the device physically vibrate?

- **YES** → The API works from console; our JS code has a timing or guard issue. Report "API works in console."
- **NO** → The OS or browser is blocking the API. Report "API does NOT work from console." This is a Samsung/Android restriction — document as known limitation.

**Test B — Feature detection:**
```js
"vibrate" in navigator
```
- **true** → API is detected (feature detection passes; vibration should be possible)
- **false** → API not detected (browser has removed it; genuine platform gap)

**Test C — App vibration (after A and B):**
1. With remote DevTools open (console visible), start a session in the app
2. Wait for a phase transition (Inhale → Hold)
3. Observe Console for any errors
4. Does the device vibrate at phase transition?

**Report back:**
- Test A result (YES/NO vibrate from console)
- Test B result (true/false feature detection)  
- Test C result (vibrates at phase transition YES/NO, any console errors)
- Any Samsung/Android settings that might explain the behavior (e.g., "Vibration intensity was off in Samsung settings")
  </how-to-verify>
  <resume-signal>
Provide diagnostic results:
- Test A (navigator.vibrate(50) in console): vibrated YES/NO
- Test B ("vibrate" in navigator): true/false
- Test C (app phase transition): vibrates YES/NO, any console errors
- Any other observations
  </resume-signal>
</task>

<task type="auto">
  <name>Task 2: Update VIBRATION-TEST-REPORT.md with diagnostic findings</name>
  <files>.planning/phases/03-critical-bugs-ux-polish/VIBRATION-TEST-REPORT.md</files>
  <read_first>.planning/phases/03-critical-bugs-ux-polish/VIBRATION-TEST-REPORT.md</read_first>
  <action>
Based on the human checkpoint results received, append a new section to VIBRATION-TEST-REPORT.md documenting the DevTools diagnostic:

```markdown
---

## Phase 3 Gap Closure Diagnostic — 2026-06-29

### Code Normalization Applied
- Replaced inline `navigator.vibrate(40)` in `showCountdown()` with `vibrate([40])` wrapper call
- All vibration calls now route through centralized `vibrate()` function

### DevTools Console Diagnostic Results

| Test | Result | Notes |
|------|--------|-------|
| `navigator.vibrate(50)` in console | [YES/NO] | [What happened] |
| `"vibrate" in navigator` | [true/false] | [Feature detection] |
| App phase transition vibration | [YES/NO] | [Console errors if any] |

### Conclusion

[Write one of the following based on results:]

**If API works from console but not in app:**
Root cause: Timing or guard condition in app code. Further investigation required.

**If API does NOT work from console:**
Root cause: Samsung One UI / Android OS browser restriction on Vibration API for web apps.
This is a known limitation of the Samsung Galaxy A33 5G on Android 16 with Chrome.
Vibration remains implemented and will work on devices where the OS permits it.
This is documented as a known device-specific limitation, not a code bug.

**If feature detection returns false:**
Root cause: Chrome on Android 16 has removed Vibration API support.
Update VIBRATION feature guard to reflect this; document as platform limitation.

### Updated Status
[RESOLVED / KNOWN_LIMITATION / NEEDS_INVESTIGATION]
```

Fill in the actual test results from the checkpoint. Update the frontmatter `status:` field from `failed` to `known_limitation` or `resolved` based on findings.
  </action>
  <verify>Grep VIBRATION-TEST-REPORT.md for "Gap Closure Diagnostic" — should return 1 match.</verify>
  <acceptance_criteria>
    - VIBRATION-TEST-REPORT.md contains a "Gap Closure Diagnostic" section with actual test results
    - The conclusion documents root cause (device restriction OR code fix applied)
    - Frontmatter status field is updated from "failed" to match conclusion
  </acceptance_criteria>
  <done>Test report updated with DevTools diagnostic results and clear conclusion on vibration gap status.</done>
</task>

</tasks>

<verification>
- [ ] index.html: `navigator.vibrate(40)` no longer exists (inline call removed from showCountdown)
- [ ] index.html: `vibrate([40])` exists in showCountdown forEach callback
- [ ] VIBRATION-TEST-REPORT.md contains "Gap Closure Diagnostic" section with filled results
- [ ] VIBRATION-TEST-REPORT.md frontmatter status updated from "failed"
- [ ] No syntax errors introduced in index.html
</verification>

<success_criteria>
- Task 1 complete: countdown vibration normalized to vibrate() wrapper
- Human checkpoint complete: DevTools diagnostic run and results provided
- Task 2 complete: VIBRATION-TEST-REPORT.md updated with diagnostic conclusion
- Vibration gap (VERIFICATION.md gap 1) either resolved or documented as known platform limitation
- Phase 3 can proceed to re-verification with all 3 gaps addressed
</success_criteria>

<output>
After completion, create `.planning/phases/03-critical-bugs-ux-polish/03-08-SUMMARY.md`
</output>
